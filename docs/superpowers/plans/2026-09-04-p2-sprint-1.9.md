# P2 Sprint 1.9（配置层收尾）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** vendor 三件接线（chemicals/fluids/thermo）+ 偏差 #4/#6/equip-lib 三项收口——PipeClass service + 5 端点、Riazi-Daubert/虚拟组分切割 service（粘度走 vendored Twu-1985）、equip-lib 沉淀 service（复用 CATEGORY_6 资产生命周期），加 Excel 批量导入满足 P2-STD-001 验收。

**Architecture:** 服务层 + API 增量 + 依赖接线，**零新表零迁移**（PipeClass/ProjectPipeClass ORM 已存在；equip-lib 复用 ConfigAsset CATEGORY_6 + content_json；Riazi 关联式以 CATEGORY_3 系数表 seed 入库）。物性估算为无状态纯函数 service；RD80/Kesler-Lee 常数有可引用来源且已数值验证（见 Task 1.9.3），粘度调 vendored `chemicals.viscosity.Twu_1985_internal`。

**Tech Stack:** FastAPI + SQLAlchemy async + Pydantic v2 + pytest（复用 conftest SQLite fixtures）

**Spec:** `spec/工艺专用综合计算软件需求规格说明书 Web版 P2.md` §3.2.5（P2-STD-001）、§3.2.6（P2-EQL-001）、§3.2.4 系数类型表（V1.2/ADR-0019 三行）；`spec/工艺专用综合计算软件需求规格说明书 Web版 P7.md` §3.2.3（3）沉淀数据标准化要求；ADR-0019。

## Global Constraints

- 角色 ACL 沿用 `require_roles`：读 DESIGNER+；写/审批 PROCESS_CONTROLLER/SYSTEM_ADMIN
- service 层异常统一 `app.services.exceptions.PcsError`（全局 handler 已接 422/404/409 信封）
- 触达文件 `ruff check` 0 错；每 Task 独立 commit；全量 pytest PASS
- 不改 ORM 已有表结构；无 Alembic 迁移
- 性能：管道等级列表 ≤300ms（PK/索引查询，天然满足）

---

### Task 1.9.0: vendor 库接线（chemicals + fluids + thermo）

**背景:** `pcs-backend/vendor/`（P0 首提交入库，355M）含 Caleb Bell 套件源码：chemicals 1.5.2 / fluids 1.3.1 / thermo 0.6.1 / ht / CoolProp。**至今零接线**（不在依赖、不在 sys.path、全仓零 import）。官方文档（[chemicals.readthedocs.io](https://chemicals.readthedocs.io/index.html)）核实定位：纯组分物性数据库 + 关联式库（33 API 模块）；**无馏分表征能力**（无 Tb+SG→MW/Tc/Pc；Riazi/Sim-Daubert/Soreide 在 vendored 源码零匹配；`thermo/phases/petroleum.py` 仅 Chao-Grayson-Streed K 值法）→ RD80 手写（Task 1.9.3）维持必要。**但有** `chemicals.viscosity.Twu_1985_internal(T, Tb, SG)`（Tb+SG→石油液体粘度 cSt）与 `chemicals.iapws`（IAPWS-95 水蒸气全套，覆盖 CATEGORY_5「CoolProp 高精度物性（水蒸气）」需求）——故接线并启用。

**Files:**
- Modify: `pcs-backend/pyproject.toml`（dependencies + `[tool.uv.sources]` path 指向 vendor）
- Test: `pcs-backend/tests/test_vendor_smoke.py`

**Interfaces:**
- Produces: `import chemicals / thermo / fluids` 可用（Task 1.9.3 粘度、P3 CHEMICAL 估算、CATEGORY_5 数据源依赖此）
- 排除: CoolProp（源码需 cmake 编译，非 path 依赖可装；水蒸气 chemicals.iapws 已覆盖，接 CoolProp 延后至确需时改用 pypi wheel）、ht（P4 管网计算时再接）

- [ ] **Step 1: 写失败冒烟测试**

```python
# pcs-backend/tests/test_vendor_smoke.py
"""vendor 接线冒烟测试（Task 1.9.0）。"""
import fluids
import chemicals
import thermo


def test_vendored_versions():
    assert chemicals.__version__ == "1.5.2"
    assert fluids.__version__ == "1.3.1"
    assert thermo.__version__ == "0.6.1"


def test_twu_petroleum_viscosity_available():
    from chemicals.viscosity import Twu_1985_internal
    nu = Twu_1985_internal(T=293.15, Tb=447.3, SG=0.73)  # n-癸烷，真值 ν20≈0.92 cSt
    assert 0.3 < nu < 3.0  # Twu 为馏分关联式，纯组分放宽带宽


def test_iapws_water_steam_available():
    from chemicals.iapws import iapws95_Psat, iapws95_Tc, iapws95_Pc
    assert abs(iapws95_Tc() - 647.096) < 0.01          # K
    assert abs(iapws95_Psat(373.15) - 101325.0) < 500  # 100°C 饱和蒸气压 ≈ 1 atm
```

- [ ] **Step 2: 确认 FAIL**（ModuleNotFoundError: fluids）

- [ ] **Step 3: pyproject 接线**

`[project] dependencies` 追加：

```toml
    "fluids>=1.3.1",
    "chemicals>=1.5.2",
    "thermo>=0.6.1",
```

追加源映射（vendor 优先于 PyPI）：

```toml
[tool.uv.sources]
fluids = { path = "vendor/fluids", editable = true }
chemicals = { path = "vendor/chemicals", editable = true }
thermo = { path = "vendor/thermo", editable = true }
```

Run: `uv sync`（锁文件更新；三件全纯 Python，无编译）

- [ ] **Step 4: 跑冒烟 PASS**（`uv run pytest tests/test_vendor_smoke.py -v`，3 PASS）

- [ ] **Step 5: 全量回归**（`uv run pytest -q` 全绿——确认接线不引入副作用）

- [ ] **Step 6: 提交**

```bash
git add pcs-backend/pyproject.toml pcs-backend/uv.lock pcs-backend/tests/test_vendor_smoke.py
git commit -m "feat(p2-s1.9): vendor 三件接线（chemicals/fluids/thermo path 依赖）+ 冒烟测试（Twu 粘度/iapws 水蒸气）"
```

---

### Task 1.9.1: PipeClass schemas + service（含作废/删除规则）

**Files:**
- Create: `pcs-backend/app/schemas/pipe_class.py`
- Create: `pcs-backend/app/services/pipe_class_service.py`
- Test: `pcs-backend/tests/services/test_pipe_class_service.py`

**Interfaces:**
- Consumes: `app.models.config_domain.PipeClass / ProjectPipeClass`（既有）；`PcsError`
- Produces（Task 1.9.2/1.9.6 依赖，签名精确如下）:
  - `PipeClassService.list_company(session, *, status=None, keyword=None) -> list[PipeClass]`
  - `PipeClassService.get(session, class_id) -> PipeClass`
  - `PipeClassService.create(session, *, payload: PipeClassCreate) -> PipeClass`
  - `PipeClassService.update(session, class_id, *, payload: PipeClassUpdate) -> PipeClass`
  - `PipeClassService.delete(session, class_id) -> None`（在用→PcsError 409）
  - `PipeClassService.assign_to_project(session, project_id, class_id, *, enabled=True, override=None) -> ProjectPipeClass`
  - `PipeClassService.list_project(session, project_id) -> list[ProjectPipeClass]`
  - Pydantic: `PipeClassCreate / PipeClassUpdate / PipeClassResponse / ProjectAssignRequest / ProjectPipeClassResponse`

- [ ] **Step 1: 写失败测试**

```python
# pcs-backend/tests/services/test_pipe_class_service.py
"""PipeClassService 单元测试（Task 1.9.1 / P2-STD-001）。"""
import pytest

from app.models.config_domain import ProjectPipeClass
from app.schemas.pipe_class import PipeClassCreate, PipeClassUpdate
from app.services.exceptions import PcsError
from app.services.pipe_class_service import PipeClassService


def _payload(class_id="A1", **kw):
    base = dict(
        class_id=class_id, class_name="管道等级 A1", material_standard="GB/T 8163",
        corrosion_allowance=1.5, design_pressure=2.5, design_temperature=200.0,
        dn_series_json={"min": 15, "max": 350},
        sch_series_json={"15": "40", "25": "40", "50": "40", "100": "40", "350": "STD"},
        flange_class="PN25", source="COMPANY_STD", version="PC-V1.0",
    )
    base.update(kw)
    return PipeClassCreate(**base)


async def test_create_and_get(db):
    pc = await PipeClassService.create(db, payload=_payload())
    assert pc.status == "DRAFT"
    got = await PipeClassService.get(db, "A1")
    assert got.class_name == "管道等级 A1"


async def test_get_missing_raises_404(db):
    with pytest.raises(PcsError) as e:
        await PipeClassService.get(db, "NOPE")
    assert e.value.status == 404


async def test_create_duplicate_raises_409(db):
    await PipeClassService.create(db, payload=_payload())
    with pytest.raises(PcsError) as e:
        await PipeClassService.create(db, payload=_payload())
    assert e.value.status == 409


async def test_create_invalid_dn_range_422(db):
    with pytest.raises(PcsError) as e:
        await PipeClassService.create(db, payload=_payload(dn_series_json={"min": 600, "max": 15}))
    assert e.value.status == 422


async def test_update_and_obsolete_one_way(db):
    pc = await PipeClassService.create(db, payload=_payload())
    up = await PipeClassService.update(db, "A1", payload=PipeClassUpdate(
        **{**_payload().model_dump(), "design_pressure": 4.0, "status": "ACTIVE"}))
    assert up.design_pressure == 4.0 and up.status == "ACTIVE"
    dead = await PipeClassService.update(db, "A1", payload=PipeClassUpdate(
        **{**_payload().model_dump(), "status": "OBSOLETE"}))
    assert dead.status == "OBSOLETE"
    with pytest.raises(PcsError) as e:  # OBSOLETE 不可改回
        await PipeClassService.update(db, "A1", payload=PipeClassUpdate(
            **{**_payload().model_dump(), "status": "ACTIVE"}))
    assert e.value.status == 409


async def test_delete_blocked_when_assigned(db, make_project):
    await PipeClassService.create(db, payload=_payload())
    proj = await make_project()
    await PipeClassService.assign_to_project(db, proj.project_id, "A1")
    with pytest.raises(PcsError) as e:
        await PipeClassService.delete(db, "A1")
    assert e.value.status == 409  # 已用等级不可删除，仅可作废


async def test_list_project_returns_assignment(db, make_project):
    await PipeClassService.create(db, payload=_payload())
    proj = await make_project()
    row = await PipeClassService.assign_to_project(db, proj.project_id, "A1")
    rows = await PipeClassService.list_project(db, proj.project_id)
    assert [r.class_id for r in rows] == ["A1"]
    assert isinstance(row, ProjectPipeClass)
```

注：`make_project` fixture 若 conftest 无 → 在本测试文件内定义（`Project(name=..., code=...)` 具体必填字段以 `app/models/project.py` 为准，执行时先读该文件）。

- [ ] **Step 2: 跑测试确认 FAIL**

Run: `cd pcs-backend && uv run pytest tests/services/test_pipe_class_service.py -v`
Expected: FAIL（ModuleNotFoundError: pipe_class）

- [ ] **Step 3: 写 schemas**

```python
# pcs-backend/app/schemas/pipe_class.py
"""PipeClass API schemas（Pydantic v2；Task 1.9.1 / P2-STD-001）。"""
from __future__ import annotations

from typing import Any, Literal
from uuid import UUID

from pydantic import BaseModel, Field, model_validator


class PipeClassBase(BaseModel):
    class_id: str = Field(..., min_length=1, max_length=20)
    class_name: str = Field(..., min_length=1, max_length=200)
    material_standard: str = Field(..., min_length=1, max_length=100)
    corrosion_allowance: float = Field(..., ge=0, description="腐蚀裕量 mm")
    design_pressure: float = Field(..., gt=0, description="MPaG")
    design_temperature: float = Field(..., description="°C")
    fluid_service: str | None = Field(None, max_length=100)
    allowable_stress_json: dict[str, Any] = Field(default_factory=dict)
    dn_series_json: dict[str, int] = Field(..., description="{min,max}")
    sch_series_json: dict[str, str] = Field(..., description="DN→Sch")
    flange_class: str = Field(..., min_length=1, max_length=20)
    fitting_type: str | None = Field(None, max_length=50)
    branch_table_json: dict[str, Any] | None = None
    source: Literal["COMPANY_STD", "PROJECT"]
    version: str = Field(..., min_length=1, max_length=50)

    @model_validator(mode="after")
    def _check_dn(self) -> "PipeClassBase":
        mn, mx = self.dn_series_json.get("min"), self.dn_series_json.get("max")
        if mn is None or mx is None or not (0 < mn < mx):
            raise ValueError("dn_series_json 需 {min,max} 且 0<min<max")
        return self


class PipeClassCreate(PipeClassBase):
    pass


class PipeClassUpdate(PipeClassBase):
    status: Literal["DRAFT", "ACTIVE", "OBSOLETE"] = "DRAFT"


class PipeClassResponse(PipeClassBase):
    status: str
    model_config = {"from_attributes": True}


class ProjectAssignRequest(BaseModel):
    class_id: str = Field(..., min_length=1, max_length=20)
    enabled: bool = True
    custom_override_json: dict[str, Any] | None = None


class ProjectPipeClassResponse(BaseModel):
    project_id: UUID
    class_id: str
    enabled: bool
    custom_override_json: dict[str, Any] | None = None
    pipe_class: PipeClassResponse | None = None
    model_config = {"from_attributes": True}
```

- [ ] **Step 4: 写 service**

```python
# pcs-backend/app/services/pipe_class_service.py
"""PipeClassService — 管道等级 CRUD + 项目分配（Task 1.9.1 / P2-STD-001）。

规则（spec §3.2.5 验收）：已用等级不可删除（仅可作废）。
在用判定：project_pipe_classes 存在关联行 或 piping_results.material_class 引用。
"""
from __future__ import annotations

import uuid

from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.models.calc import PipingResult
from app.models.config_domain import PipeClass, ProjectPipeClass
from app.schemas.pipe_class import PipeClassCreate, PipeClassUpdate
from app.services.exceptions import PcsError

_STATUS_OK = {"DRAFT", "ACTIVE", "OBSOLETE"}


class PipeClassService:
    @classmethod
    async def list_company(
        cls, session: AsyncSession, *, status: str | None = None, keyword: str | None = None
    ) -> list[PipeClass]:
        stmt = select(PipeClass).where(PipeClass.source == "COMPANY_STD")
        if status:
            stmt = stmt.where(PipeClass.status == status)
        if keyword:
            stmt = stmt.where(PipeClass.class_name.ilike(f"%{keyword}%"))
        stmt = stmt.order_by(PipeClass.class_id)
        return list((await session.execute(stmt)).scalars())

    @classmethod
    async def get(cls, session: AsyncSession, class_id: str) -> PipeClass:
        pc = await session.get(PipeClass, class_id)
        if not pc:
            raise PcsError(f"管道等级 {class_id} 不存在", code="PIPE_CLASS_NOT_FOUND", status=404)
        return pc

    @classmethod
    async def create(cls, session: AsyncSession, *, payload: PipeClassCreate) -> PipeClass:
        if await session.get(PipeClass, payload.class_id):
            raise PcsError(f"管道等级 {payload.class_id} 已存在", code="PIPE_CLASS_DUP", status=409)
        pc = PipeClass(**payload.model_dump(), status="DRAFT")
        session.add(pc)
        await session.commit()
        return pc

    @classmethod
    async def update(
        cls, session: AsyncSession, class_id: str, *, payload: PipeClassUpdate
    ) -> PipeClass:
        pc = await cls.get(session, class_id)
        if payload.status == "OBSOLETE" and pc.status != "OBSOLETE":
            new_status = "OBSOLETE"  # 作废：单向，允许 DRAFT/ACTIVE → OBSOLETE
        elif payload.status == pc.status or (pc.status == "DRAFT" and payload.status == "ACTIVE"):
            new_status = payload.status
        else:
            raise PcsError(
                f"非法状态流转 {pc.status}→{payload.status}（OBSOLETE 单向）",
                code="PIPE_CLASS_BAD_TRANSITION", status=409,
            )
        data = payload.model_dump(exclude={"status"})
        for k, v in data.items():
            setattr(pc, k, v)
        pc.status = new_status
        await session.commit()
        return pc

    @classmethod
    async def delete(cls, session: AsyncSession, class_id: str) -> None:
        await cls.get(session, class_id)
        assigned = (await session.execute(
            select(ProjectPipeClass.class_id).where(ProjectPipeClass.class_id == class_id)
        )).first()
        lined = (await session.execute(
            select(PipingResult.pipe_id).where(PipingResult.material_class == class_id).limit(1)
        )).first()
        if assigned or lined:
            raise PcsError(
                f"等级 {class_id} 已被项目/管道行引用，不可删除（仅可作废）",
                code="PIPE_CLASS_IN_USE", status=409,
            )
        await session.execute(
            PipeClass.__table__.delete().where(PipeClass.class_id == class_id)
        )
        await session.commit()

    @classmethod
    async def assign_to_project(
        cls, session: AsyncSession, project_id: uuid.UUID, class_id: str,
        *, enabled: bool = True, override: dict | None = None,
    ) -> ProjectPipeClass:
        await cls.get(session, class_id)
        row = await session.get(ProjectPipeClass, {"project_id": project_id, "class_id": class_id})
        if row:
            row.enabled = enabled
            row.custom_override_json = override
        else:
            row = ProjectPipeClass(
                project_id=project_id, class_id=class_id,
                enabled=enabled, custom_override_json=override,
            )
            session.add(row)
        await session.commit()
        return row

    @classmethod
    async def list_project(cls, session: AsyncSession, project_id: uuid.UUID) -> list[ProjectPipeClass]:
        stmt = (
            select(ProjectPipeClass)
            .where(ProjectPipeClass.project_id == project_id)
            .order_by(ProjectPipeClass.class_id)
        )
        return list((await session.execute(stmt)).scalars())
```

注意：`PipingResult` 若实际类名不同（`app/models/calc.py` L33 `__tablename__ = "piping_results"`），以文件内 class 名为准，import 对应类。

- [ ] **Step 5: 跑测试确认 PASS**

Run: `uv run pytest tests/services/test_pipe_class_service.py -v`
Expected: 8 PASS

- [ ] **Step 6: 提交**

```bash
git add pcs-backend/app/schemas/pipe_class.py pcs-backend/app/services/pipe_class_service.py pcs-backend/tests/services/test_pipe_class_service.py
git commit -m "feat(p2-s1.9): PipeClassService + schemas（CRUD/作废单向/在用不可删/项目分配）"
```

---

### Task 1.9.2: 管道等级 API 端点（spec 5 端点 + DELETE/分配）

**Files:**
- Create: `pcs-backend/app/api/v1/pipe_classes.py`
- Modify: `pcs-backend/app/api/v1/__init__.py`（注册 router）
- Test: `pcs-backend/tests/api/v1/test_pipe_classes.py`

**Interfaces:**
- Consumes: Task 1.9.1 全部 service 方法与 schemas；`current_actor / require_roles / get_db`（config.py 同款）
- Produces: 路由 `GET/POST /api/v1/pipe-classes`、`GET/PUT/DELETE /api/v1/pipe-classes/{class_id}`、`GET/POST /api/v1/projects/{project_id}/pipe-classes`

- [ ] **Step 1: 写失败测试**（模式抄 `tests/api/v1/test_config.py`：client + sample_user_token/sample_pc_token）

```python
# pcs-backend/tests/api/v1/test_pipe_classes.py
"""管道等级 API 测试（Task 1.9.2 / spec §3.2.5 5 端点 + 删除/分配）。"""

_BODY = {
    "class_id": "A1", "class_name": "管道等级 A1", "material_standard": "GB/T 8163",
    "corrosion_allowance": 1.5, "design_pressure": 2.5, "design_temperature": 200.0,
    "dn_series_json": {"min": 15, "max": 350},
    "sch_series_json": {"15": "40", "350": "STD"},
    "flange_class": "PN25", "source": "COMPANY_STD", "version": "PC-V1.0",
}


async def _auth(tok):
    return {"Authorization": f"Bearer {tok}"}


async def test_crud_roundtrip(client, sample_pc_token):
    r = await client.post("/api/v1/pipe-classes", json=_BODY, headers=await _auth(sample_pc_token))
    assert r.status_code == 201 and r.json()["status"] == "DRAFT"

    r = await client.get("/api/v1/pipe-classes", headers=await _auth(sample_pc_token))
    assert r.status_code == 200 and [c["class_id"] for c in r.json()] == ["A1"]

    r = await client.get("/api/v1/pipe-classes/A1", headers=await _auth(sample_pc_token))
    assert r.status_code == 200

    r = await client.put("/api/v1/pipe-classes/A1", json={**_BODY, "status": "ACTIVE"},
                         headers=await _auth(sample_pc_token))
    assert r.status_code == 200 and r.json()["status"] == "ACTIVE"

    r = await client.delete("/api/v1/pipe-classes/A1", headers=await _auth(sample_pc_token))
    assert r.status_code == 204  # 未被引用 → 可删

    r = await client.get("/api/v1/pipe-classes/A1", headers=await _auth(sample_pc_token))
    assert r.status_code == 404


async def test_delete_in_use_409(client, sample_pc_token, sample_project):
    await client.post("/api/v1/pipe-classes", json=_BODY, headers=await _auth(sample_pc_token))
    r = await client.post(
        f"/api/v1/projects/{sample_project.project_id}/pipe-classes",
        json={"class_id": "A1"}, headers=await _auth(sample_pc_token))
    assert r.status_code == 201
    r = await client.delete("/api/v1/pipe-classes/A1", headers=await _auth(sample_pc_token))
    assert r.status_code == 409  # 已用等级不可删除


async def test_project_list(client, sample_pc_token, sample_project):
    r = await client.get(f"/api/v1/projects/{sample_project.project_id}/pipe-classes",
                         headers=await _auth(sample_pc_token))
    assert r.status_code == 200 and r.json() == []


async def test_create_requires_pc_role(client, sample_user_token):
    r = await client.post("/api/v1/pipe-classes", json=_BODY, headers=await _auth(sample_user_token))
    assert r.status_code == 403


async def test_list_denied_anonymous(client):
    r = await client.get("/api/v1/pipe-classes")
    assert r.status_code in (401, 403)
```

`sample_project` fixture：conftest 若无则在 test_config.py 风格上补（建 `Project` 最小行）。执行时读 `app/models/project.py` 确认必填列。

- [ ] **Step 2: 确认 FAIL**（404 路由不存在）

- [ ] **Step 3: 写 router**

```python
# pcs-backend/app/api/v1/pipe_classes.py
"""管道等级端点（Task 1.9.2 / P2-STD-001）。

spec §3.2.5 API 表 5 端点 + 验收派生的 DELETE（在用 409 仅可作废）与项目分配 POST。
"""
from typing import Annotated
from uuid import UUID

from fastapi import APIRouter, Depends, Query
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.deps import get_db
from app.api.v1.auth import current_actor, require_roles  # 与 config.py 同源，实际导入路径以 config.py 头部为准
from app.schemas.pipe_class import (
    ProjectAssignRequest, ProjectPipeClassResponse, PipeClassCreate,
    PipeClassResponse, PipeClassUpdate,
)
from app.services.pipe_class_service import PipeClassService

router = APIRouter(tags=["pipe-classes"])


@router.get("/pipe-classes", response_model=list[PipeClassResponse])
async def list_pipe_classes(
    user: Annotated[object, Depends(current_actor)],
    db: Annotated[AsyncSession, Depends(get_db)],
    status: str | None = Query(None), keyword: str | None = Query(None),
):
    require_roles(user, "DESIGNER", "PROCESS_CONTROLLER", "SYSTEM_ADMIN")
    return await PipeClassService.list_company(db, status=status, keyword=keyword)


@router.post("/pipe-classes", response_model=PipeClassResponse, status_code=201)
async def create_pipe_class(
    payload: PipeClassCreate,
    user: Annotated[object, Depends(current_actor)],
    db: Annotated[AsyncSession, Depends(get_db)],
):
    require_roles(user, "PROCESS_CONTROLLER", "SYSTEM_ADMIN")
    return await PipeClassService.create(db, payload=payload)


@router.get("/pipe-classes/{class_id}", response_model=PipeClassResponse)
async def get_pipe_class(
    class_id: str,
    user: Annotated[object, Depends(current_actor)],
    db: Annotated[AsyncSession, Depends(get_db)],
):
    require_roles(user, "DESIGNER", "PROCESS_CONTROLLER", "SYSTEM_ADMIN")
    return await PipeClassService.get(db, class_id)


@router.put("/pipe-classes/{class_id}", response_model=PipeClassResponse)
async def update_pipe_class(
    class_id: str, payload: PipeClassUpdate,
    user: Annotated[object, Depends(current_actor)],
    db: Annotated[AsyncSession, Depends(get_db)],
):
    require_roles(user, "PROCESS_CONTROLLER", "SYSTEM_ADMIN")
    return await PipeClassService.update(db, class_id, payload=payload)


@router.delete("/pipe-classes/{class_id}", status_code=204)
async def delete_pipe_class(
    class_id: str,
    user: Annotated[object, Depends(current_actor)],
    db: Annotated[AsyncSession, Depends(get_db)],
):
    require_roles(user, "PROCESS_CONTROLLER", "SYSTEM_ADMIN")
    await PipeClassService.delete(db, class_id)


@router.get(
    "/projects/{project_id}/pipe-classes", response_model=list[ProjectPipeClassResponse]
)
async def list_project_pipe_classes(
    project_id: UUID,
    user: Annotated[object, Depends(current_actor)],
    db: Annotated[AsyncSession, Depends(get_db)],
):
    require_roles(user, "DESIGNER", "PROCESS_CONTROLLER", "SYSTEM_ADMIN")
    return await PipeClassService.list_project(db, project_id)


@router.post(
    "/projects/{project_id}/pipe-classes",
    response_model=ProjectPipeClassResponse, status_code=201,
)
async def assign_project_pipe_class(
    project_id: UUID, payload: ProjectAssignRequest,
    user: Annotated[object, Depends(current_actor)],
    db: Annotated[AsyncSession, Depends(get_db)],
):
    require_roles(user, "PROCESS_CONTROLLER", "SYSTEM_ADMIN")
    return await PipeClassService.assign_to_project(
        db, project_id, payload.class_id,
        enabled=payload.enabled, override=payload.custom_override_json,
    )
```

`__init__.py` 注册：

```python
from app.api.v1.pipe_classes import router as pipe_classes_router
api_router.include_router(pipe_classes_router)
```

执行注意：`current_actor/require_roles` 的真实导入路径、`_Actor` 类型别名——照抄 `app/api/v1/config.py` 头部写法（含 `Annotated[_Actor, ...]`），不要自造。

- [ ] **Step 4: 跑测试确认 PASS**（`uv run pytest tests/api/v1/test_pipe_classes.py -v`，5 PASS）

- [ ] **Step 5: 全量回归 + ruff**（`uv run pytest -q` 全绿 / 触达文件 0 错）

- [ ] **Step 6: 提交**

```bash
git add pcs-backend/app/api/v1/pipe_classes.py pcs-backend/app/api/v1/__init__.py pcs-backend/tests/api/v1/test_pipe_classes.py
git commit -m "feat(p2-s1.9): 管道等级 5 端点 + DELETE 在用保护 + 项目级分配（P2-STD-001）"
```

---

### Task 1.9.3: Riazi-Daubert 物性估算 + 虚拟组分切割 service

**Files:**
- Create: `pcs-backend/app/services/petroleum_service.py`
- Test: `pcs-backend/tests/services/test_petroleum_service.py`

**Interfaces:**
- Consumes: `chemicals.viscosity.Twu_1985_internal`（Task 1.9.0 接线后可用——粘度走库，不自写）
- Produces（P3 手动创建向导 / Task 1.9.4 seed 引用）:
  - `PetroleumService.estimate(tb_c: float, sg: float) -> dict`（键：`mw, tc_k, pc_mpa, omega, tb_k, nu_20c_cst`）
  - `PetroleumService.vapor_pressure(tb_c: float, sg: float, t_c: float) -> float`（MPa）
  - `PetroleumService.cut_pseudo_components(t_start_c, t_end_c, *, cut_width_c=25.0, sg=None) -> list[dict]`（每项 `{index, tb_start_c, tb_end_c, tb_mid_c, sg, **estimate}`）
  - 模块常量 `RD80_CONSTANTS: dict[str, dict[str, float]]`

**常数来源（已数值验证，执行者照抄勿改）：**
- Riazi-Daubert (1980) 广义式 θ = a·Tb(°R)^b·SG^c（[Hydrocarbon Processing 59:115](https://hal.science/hal-01707454/document) Table 1 反演 + n-癸烷数值验证）:
  - MW = 4.5673×10⁻⁵ · Tb^2.1962 · SG^−1.0164
  - Tc(°R) = 24.078 · Tb^0.58848 · SG^0.35936
  - Pc(psia) = 3.1228×10⁹ · Tb^−2.3125 · SG^2.3201
- Kesler-Lee 偏心因子（Tb 处求值）与蒸汽压（[Lee-Kesler 1975，经 HAL 论文 Eq.10-12 与 n-癸烷 ω=0.4936 vs 真值 0.4923 闭环验证]）:
  - f0(T) = 5.92714 − 6.09648/Tr − 1.28862·ln Tr + 0.169347·Tr⁶
  - f1(T) = 15.2518 − 15.6875/Tr − 13.4721·ln Tr + 0.43577·Tr⁶
  - ω = [ln(1.01325/Pc_bar) − f0(Tbr)] / f1(Tbr)
  - ln Pr(T) = f0(Tr) + ω·f1(Tr) → Psat = Pr·Pc

- [ ] **Step 1: 写失败测试**

```python
# pcs-backend/tests/services/test_petroleum_service.py
"""Riazi-Daubert 估算 + 虚拟组分切割测试（Task 1.9.3 / ADR-0019 / P2 §3.2.4）。

基准：n-癸烷 Tb=174.1°C SG=0.73，文献真值 MW=142.3 / Tc=617.7K / Pc=2.11MPa / ω=0.4923。
广义关联式对纯组分典型偏差 5-10%，断言带宽按此放宽。
"""
import pytest

from app.services.exceptions import PcsError
from app.services.petroleum_service import PetroleumService


def test_estimate_n_decane():
    r = PetroleumService.estimate(174.1, 0.73)
    assert 140 <= r["mw"] <= 165          # 计算值 ≈152
    assert 600 <= r["tc_k"] <= 630        # ≈613.5
    assert 1.7 <= r["pc_mpa"] <= 2.3      # ≈1.98
    assert 0.3 <= r["omega"] <= 0.7       # ≈0.494
    assert r["tb_k"] == pytest.approx(447.25, abs=0.1)
    assert 0.3 <= r["nu_20c_cst"] <= 3.0  # Twu(1985) 真值 ≈0.92 cSt（馏分式放宽带宽）


def test_estimate_input_validation():
    with pytest.raises(PcsError):
        PetroleumService.estimate(174.1, 1.5)   # SG 越界
    with pytest.raises(PcsError):
        PetroleumService.estimate(1000.0, 0.8)  # Tb 越界


def test_vapor_pressure_positive_and_below_pc():
    p = PetroleumService.vapor_pressure(174.1, 0.73, 100.0)
    assert 0.0 < p < 1.7  # 常识：癸烷 100°C 饱和蒸气压 ~0.02 MPa 量级，且远低于 Pc


def test_cut_default_25c():
    cuts = PetroleumService.cut_pseudo_components(150.0, 260.0)
    widths = {round(c["tb_end_c"] - c["tb_start_c"], 6) for c in cuts[:-1]}
    assert widths == {25.0}          # 中间段恒 25
    assert len(cuts) >= 4            # 110/25 → 至少 5 段（ceil）
    assert cuts[0]["tb_start_c"] == 150.0 and cuts[-1]["tb_end_c"] == 260.0
    assert cuts[-1]["tb_end_c"] - cuts[-1]["tb_start_c"] <= 25.0 + 1e-9  # 末段吸收余数 ≤25
    assert all("mw" in c and "tc_k" in c for c in cuts)  # 每段自带估算物性


def test_cut_custom_width_and_validation():
    cuts = PetroleumService.cut_pseudo_components(100.0, 140.0, cut_width_c=20.0)
    assert len(cuts) == 2
    with pytest.raises(PcsError):
        PetroleumService.cut_pseudo_components(200.0, 100.0)  # start >= end
    with pytest.raises(PcsError):
        PetroleumService.cut_pseudo_components(100.0, 200.0, cut_width_c=0)
```

- [ ] **Step 2: 确认 FAIL**

- [ ] **Step 3: 写实现**

```python
# pcs-backend/app/services/petroleum_service.py
"""PetroleumService — 炼油馏分物性估算 + 虚拟组分切割（Task 1.9.3 / ADR-0019）。

关联式常数来源（执行者勿改数值）：
- RD80 广义式 θ = a·Tb(°R)^b·SG^c：Riazi & Daubert (1980) Hydrocarbon Processing 59:115；
  本文件常数为文献值，已对 n-癸烷（Tb 174.1°C/SG 0.73）数值验证。
- Kesler-Lee f0/f1：Lee & Kesler (1975) AIChE J 21:510（ω 与蒸汽压通用式）。
单位约定：入参/出参外部一律 °C、MPa；内部换算 °R、psia、bar。
"""
from __future__ import annotations

import math

from app.services.exceptions import PcsError

RD80_CONSTANTS: dict[str, dict[str, float]] = {
    # property: (a, b, c) — Tb 用 °R，结果单位见 _CONVERT
    "mw": {"a": 4.5673e-5, "b": 2.1962, "c": -1.0164},   # g/mol
    "tc": {"a": 24.078, "b": 0.58848, "c": 0.35936},      # °R
    "pc": {"a": 3.1228e9, "b": -2.3125, "c": 2.3201},     # psia
}

_SG_RANGE = (0.5, 1.2)
_TB_C_RANGE = (-100.0, 800.0)


def _f0(tr: float) -> float:
    return 5.92714 - 6.09648 / tr - 1.28862 * math.log(tr) + 0.169347 * tr**6


def _f1(tr: float) -> float:
    return 15.2518 - 15.6875 / tr - 13.4721 * math.log(tr) + 0.43577 * tr**6


class PetroleumService:
    @classmethod
    def _validate(cls, tb_c: float, sg: float) -> None:
        if not (_SG_RANGE[0] < sg < _SG_RANGE[1]):
            raise PcsError(f"SG 越界 {sg}（有效 {_SG_RANGE}）", code="PETRO_INVALID_SG", status=422)
        if not (_TB_C_RANGE[0] < tb_c < _TB_C_RANGE[1]):
            raise PcsError(f"Tb 越界 {tb_c}°C（有效 {_TB_C_RANGE}）", code="PETRO_INVALID_TB", status=422)

    @classmethod
    def estimate(cls, tb_c: float, sg: float) -> dict:
        """馏程沸点(°C) + 比重 → MW/Tc/Pc/ω/粘度（RD80 + Kesler-Lee + Twu-1985）。"""
        from chemicals.viscosity import Twu_1985_internal  # vendor（Task 1.9.0 接线）

        cls._validate(tb_c, sg)
        tb_r = (tb_c + 273.15) * 9.0 / 5.0
        mw = RD80_CONSTANTS["mw"]["a"] * tb_r ** RD80_CONSTANTS["mw"]["b"] * sg ** RD80_CONSTANTS["mw"]["c"]
        tc_r = RD80_CONSTANTS["tc"]["a"] * tb_r ** RD80_CONSTANTS["tc"]["b"] * sg ** RD80_CONSTANTS["tc"]["c"]
        pc_psia = RD80_CONSTANTS["pc"]["a"] * tb_r ** RD80_CONSTANTS["pc"]["b"] * sg ** RD80_CONSTANTS["pc"]["c"]
        tc_k = tc_r * 5.0 / 9.0
        pc_bar = pc_psia / 14.5038
        pc_mpa = pc_bar / 10.0
        tbr = (tb_c + 273.15) / tc_k
        omega = (math.log(1.01325 / pc_bar) - _f0(tbr)) / _f1(tbr)
        nu_20 = Twu_1985_internal(T=293.15, Tb=tb_c + 273.15, SG=sg)
        return {"tb_k": tb_c + 273.15, "mw": mw, "tc_k": tc_k, "pc_mpa": pc_mpa,
                "omega": omega, "nu_20c_cst": nu_20}

    @classmethod
    def vapor_pressure(cls, tb_c: float, sg: float, t_c: float) -> float:
        """Lee-Kesler：ln Pr = f0(Tr) + ω·f1(Tr)，返回 MPa。"""
        est = cls.estimate(tb_c, sg)
        tr = (t_c + 273.15) / est["tc_k"]
        if not (0.3 < tr < 1.0):
            raise PcsError(f"Tr={tr:.3f} 超出蒸汽压式适用域 (0.3,1.0)", code="PETRO_TR_RANGE", status=422)
        ln_pr = _f0(tr) + est["omega"] * _f1(tr)
        pr = math.exp(ln_pr)
        return pr * est["pc_mpa"]

    @classmethod
    def cut_pseudo_components(
        cls, t_start_c: float, t_end_c: float, *, cut_width_c: float = 25.0, sg: float | None = None
    ) -> list[dict]:
        """沸程切割（ADR-0019：默认 25°C/段，末段吸收余数）。

        sg 缺省 0.85（P2 简化：全段共用整体 SG；P3 向导可传分段 SG）。
        """
        if t_start_c >= t_end_c:
            raise PcsError("切割起点须小于终点", code="PETRO_BAD_RANGE", status=422)
        if cut_width_c <= 0:
            raise PcsError("切割宽度须 >0", code="PETRO_BAD_WIDTH", status=422)
        sg = 0.85 if sg is None else sg
        edges = []
        t = t_start_c
        while t < t_end_c - 1e-9:
            edges.append(t)
            t += cut_width_c
        edges.append(t_end_c)  # 末段吸收余数
        cuts = []
        for i in range(len(edges) - 1):
            mid = (edges[i] + edges[i + 1]) / 2.0
            cuts.append({
                "index": i, "tb_start_c": edges[i], "tb_end_c": edges[i + 1],
                "tb_mid_c": mid, "sg": sg, **cls.estimate(mid, sg),
            })
        return cuts
```

- [ ] **Step 4: 跑测试确认 PASS**（6 PASS；若 n-癸烷断言带宽擦边，先复核实现单位换算，不得放宽断言了事）

- [ ] **Step 5: 提交**

```bash
git add pcs-backend/app/services/petroleum_service.py pcs-backend/tests/services/test_petroleum_service.py
git commit -m "feat(p2-s1.9): Riazi-Daubert 物性估算 + Kesler-Lee ω/蒸汽压 + 虚拟组分切割（ADR-0019）"
```

---

### Task 1.9.4: CATEGORY_3 三张 seed 表 + 增量 seed 改造

**Files:**
- Modify: `pcs-backend/app/seeds/category3_defaults.json`（追加 3 条）
- Modify: `pcs-backend/app/services/coefficient_service.py`（seed 幂等改按 name 增量）
- Test: `pcs-backend/tests/seeds/test_category3_seeds.py`（追加用例）

**Interfaces:**
- Produces: seed 后 `CoefficientTable` 含 `petroleum_correlations / property_estimation_params / pseudo_component_cutting`（Task 1.9.5 search 不依赖此；P3 物流向导读取）

- [ ] **Step 1: 追加失败测试**

```python
# 追加到 tests/seeds/test_category3_seeds.py
async def test_seed_adds_v12_petroleum_tables():
    factory = get_async_session_factory()
    async with factory() as session:
        names = {n for (n,) in await session.execute(select(CoefficientTable.name))}
    assert "petroleum_correlations" in names
    assert "property_estimation_params" in names
    assert "pseudo_component_cutting" in names
```

（沿用该文件既有的 engine/session 复用与清理模式——执行时照抄邻近测试。）

- [ ] **Step 2: 确认 FAIL**

- [ ] **Step 3: JSON 追加 3 条**（结构对齐现有条目；常数与 Task 1.9.3 `RD80_CONSTANTS` 一字不差）

```json
{
  "name": "petroleum_correlations",
  "applicable_range": "PETROLEUM",
  "data_json": {
    "description": "炼油物性关联式（V1.2 ADR-0019）：馏程/比重→分子量/临界参数/偏心因子/蒸汽压",
    "method": "Riazi-Daubert (1980) generalized θ=a·Tb(°R)^b·SG^c + Kesler-Lee (1975) f0/f1",
    "constants": {
      "mw": {"a": 4.5673e-05, "b": 2.1962, "c": -1.0164, "unit": "g/mol"},
      "tc": {"a": 24.078, "b": 0.58848, "c": 0.35936, "unit": "degR"},
      "pc": {"a": 3.1228e+09, "b": -2.3125, "c": 2.3201, "unit": "psia"}
    },
    "kessler_lee": {
      "f0": [5.92714, -6.09648, -1.28862, 0.169347],
      "f1": [15.2518, -15.6875, -13.4721, 0.43577],
      "form": "f = c0 + c1/Tr + c2·lnTr + c3·Tr^6"
    },
    "viscosity": {"method": "chemicals.viscosity.Twu_1985_internal (vendor)", "unit": "cSt@20°C"},
    "out_of_scope": ["vc", "zc"]
  }
}
```

`property_estimation_params`：`{"default_method": "chemicals/thermo", "estimated_flag_rule": "estimated=true", "group_contribution": "UNIFAC"}`；
`pseudo_component_cutting`：`{"default_cut_width_c": 25.0, "boundary_rule": "last_cut_absorbs_remainder", "default_sg": 0.85}`。
（其余字段 `version/status/std_source` 等对齐现有 6 条的键结构。）

- [ ] **Step 4: seed 幂等改增量**——`coefficient_service.seed_default_tables` 现为"任一行存在即整体跳过"，改为按 name 缺失才插（保留 direct ORM add + `asset_id=None` + `status="PUBLISHED"` 语义，只改判重粒度）。

- [ ] **Step 5: 跑 seed 测试 PASS**（`uv run pytest tests/seeds/test_category3_seeds.py -v`；老用例仍须全绿——增量语义不得破坏）

- [ ] **Step 6: 提交**

```bash
git add pcs-backend/app/seeds/category3_defaults.json pcs-backend/app/services/coefficient_service.py pcs-backend/tests/seeds/test_category3_seeds.py
git commit -m "feat(p2-s1.9): CATEGORY_3 增 seed 三表（炼油关联式/物性估算参数/切割规则）+ 增量幂等"
```

---

### Task 1.9.5: equip-lib 沉淀 service + search/settle 端点（CATEGORY_6 复用）

**Files:**
- Create: `pcs-backend/app/schemas/equip_lib.py`
- Create: `pcs-backend/app/services/equip_lib_service.py`
- Create: `pcs-backend/app/api/v1/equip_lib.py`
- Modify: `pcs-backend/app/api/v1/__init__.py`
- Test: `pcs-backend/tests/api/v1/test_equip_lib.py`

**Interfaces:**
- Consumes: `ConfigAsset`（category="CATEGORY_6"）+ 既有 `/config/assets/{id}/submit|approve|publish` 审批链；`make_asset` fixture 模式
- Produces:
  - `EquipLibSettleRequest`（Pydantic，标准化校验内嵌）
  - `EquipLibService.settle(session, *, payload, source_equipment_id, source_project_id, created_by) -> ConfigAsset`（status=DRAFT，等待走 CONFIG 审批；PUBLISH 即入库生效）
  - `EquipLibService.search(session, *, keyword, equipment_type, limit) -> list[ConfigAsset]`（仅 PUBLISHED）
  - 端点 `GET /api/v1/equip-lib/search`、`POST /api/v1/equip-lib/settle`

- [ ] **Step 1: 写失败测试**

```python
# pcs-backend/tests/api/v1/test_equip_lib.py
"""equip-lib 沉淀 + 检索测试（Task 1.9.5 / P2-EQL-001，CATEGORY_6 复用审批链）。"""
import pytest


_SETTLE = {
    "equipment_name": "原料进料泵",
    "equipment_type": "PUMP",
    "standard_drawing_no": "AB-1234",
    "applicable_conditions": {"pressure_mpa": "0.5~2.5", "temperature_c": "-20~150", "medium": "蜡油"},
    "material": "ZG230-450",
    "weight_kg": 850.0,
    "key_dimensions": {"head_m": 120, "flow_m3h": 45, "rpm": 2950},
    "original_tag": "121-P-101A",
    "commissioning_date": "2015-06-30",
    "source_equipment_id": "00000000-0000-0000-0000-000000000001",
    "source_project_id": "00000000-0000-0000-0000-000000000002",
}


async def _h(tok):
    return {"Authorization": f"Bearer {tok}"}


async def test_settle_creates_category6_draft(client, sample_pc_token):
    r = await client.post("/api/v1/equip-lib/settle", json=_SETTLE, headers=await _h(sample_pc_token))
    assert r.status_code == 201
    body = r.json()
    assert body["category"] == "CATEGORY_6" and body["status"] == "DRAFT"


async def test_settle_validation_missing_material(client, sample_pc_token):
    bad = {k: v for k, v in _SETTLE.items() if k != "material"}
    r = await client.post("/api/v1/equip-lib/settle", json=bad, headers=await _h(sample_pc_token))
    assert r.status_code == 422  # 材质必填（P7 §3.2.3(4) 标准化要求）


async def test_search_returns_only_published(client, sample_pc_token, db, make_asset):
    # 一条 PUBLISHED + 一条 DRAFT
    from app.models.enums import ConfigStatus
    pub = await make_asset(status=ConfigStatus.PUBLISHED.value, category="CATEGORY_6", name="循环水泵 X")
    draft = await make_asset(status=ConfigStatus.DRAFT.value, category="CATEGORY_6", name="循环水泵 Y")
    r = await client.get("/api/v1/equip-lib/search", params={"keyword": "循环水泵"},
                         headers=await _h(sample_pc_token))
    assert r.status_code == 200
    ids = [a["asset_id"] for a in r.json()]
    assert str(pub.asset_id) in ids and str(draft.asset_id) not in ids


async def test_full_settle_lifecycle_publishes(client, sample_pc_token, db):
    """沉淀申请 → CONFIG 审批链 submit/approve/publish → 入库生效。"""
    r = await client.post("/api/v1/equip-lib/settle", json=_SETTLE, headers=await _h(sample_pc_token))
    asset_id = r.json()["asset_id"]
    await client.post(f"/api/v1/config/assets/{asset_id}/submit", headers=await _h(sample_pc_token))
    await client.post(f"/api/v1/config/assets/{asset_id}/approve", headers=await _h(sample_pc_token))
    r = await client.post(f"/api/v1/config/assets/{asset_id}/publish", headers=await _h(sample_pc_token))
    assert r.status_code == 200 and r.json()["status"] == "PUBLISHED"
    r = await client.get("/api/v1/equip-lib/search", params={"keyword": "原料进料泵"},
                         headers=await _h(sample_pc_token))
    assert [a["asset_id"] for a in r.json()] == [asset_id]  # 沉淀记录与源项目解耦（仅存快照）
```

（`make_asset` 支持 name/category/status 参数——若现 fixture 签名不同，扩展 fixture 而非绕过。）

- [ ] **Step 2: 确认 FAIL**

- [ ] **Step 3: schemas + service + router**

```python
# pcs-backend/app/schemas/equip_lib.py
"""equip-lib 沉淀 schemas（Task 1.9.5 / P2-EQL-001 + P7 §3.2.3(4) 标准化要求）。"""
from __future__ import annotations

from pydantic import BaseModel, Field


class ApplicableConditions(BaseModel):
    pressure_mpa: str | None = None
    temperature_c: str | None = None
    medium: str | None = None


class EquipLibSettleRequest(BaseModel):
    """标准化信息校验规则（必填项照 P7 表）：材质/关键尺寸/原项目位号/投用日期必填；
    标准图号如有则必填→简化为可选字段+非空校验由前端承担（P2）；重量必填（如有）。
    """

    equipment_name: str = Field(..., min_length=1, max_length=200)
    equipment_type: str = Field(..., min_length=1, max_length=30)
    standard_drawing_no: str | None = Field(None, max_length=100)
    applicable_conditions: ApplicableConditions | None = None
    material: str = Field(..., min_length=1, max_length=200)
    weight_kg: float | None = Field(None, gt=0)
    key_dimensions: dict = Field(..., description="关键尺寸，必填（至少一键）")
    original_tag: str = Field(..., min_length=1, max_length=50)
    commissioning_date: str = Field(..., description="投用日期 YYYY-MM-DD")
    source_equipment_id: str | None = None
    source_project_id: str | None = None

    @classmethod
    def require_any_dimension(cls, v: dict) -> dict:
        if not v:
            raise ValueError("key_dimensions 不能为空")
        return v
```

```python
# pcs-backend/app/services/equip_lib_service.py
"""EquipLibService — 设备沉淀（CATEGORY_6）+ 检索（Task 1.9.5）。

沉淀 = 建 CATEGORY_6 ConfigAsset（content_json 为标准化信息快照，与源项目解耦），
审批复用 /config/assets 既有 submit/approve/publish；PUBLISH 即入库生效。
"""
from __future__ import annotations

from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.models.config_domain import ConfigAsset
from app.schemas.equip_lib import EquipLibSettleRequest


class EquipLibService:
    @classmethod
    async def settle(cls, session: AsyncSession, *, payload: EquipLibSettleRequest) -> ConfigAsset:
        asset = ConfigAsset(
            category="CATEGORY_6",
            name=f"{payload.equipment_name} [{payload.original_tag}]",
            description=f"{payload.equipment_type} 沉淀快照（源位号 {payload.original_tag}）",
            current_version="settle-v1",
            status="DRAFT",
            content_json={
                "equipment_type": payload.equipment_type,
                "standard_info": payload.model_dump(exclude={"equipment_name", "equipment_type"}),
            },
        )
        session.add(asset)
        # 沉淀版本行：与 ConfigVersion 语义对齐（config_service 创建 asset 时同款两步）
        await session.flush()
        from app.models.config_domain import ConfigVersion
        session.add(ConfigVersion(
            asset_id=asset.asset_id, version_code="settle-v1",
            content_json=asset.content_json, status="DRAFT",
        ))
        await session.commit()
        return asset

    @classmethod
    async def search(
        cls, session: AsyncSession, *, keyword: str | None = None,
        equipment_type: str | None = None, limit: int = 50,
    ) -> list[ConfigAsset]:
        stmt = select(ConfigAsset).where(
            ConfigAsset.category == "CATEGORY_6", ConfigAsset.status == "PUBLISHED"
        )
        if keyword:
            stmt = stmt.where(ConfigAsset.name.ilike(f"%{keyword}%"))
        if equipment_type:
            stmt = stmt.where(
                ConfigAsset.content_json["equipment_type"].as_string() == equipment_type
            )
        return list((await session.execute(stmt.limit(min(limit, 200)))).scalars())
```

router（同 Task 1.9.2 模式，`/equip-lib/search` GET、`/equip-lib/settle` POST，写角色 PROCESS_CONTROLLER/SYSTEM_ADMIN，读 DESIGNER+，response_model 复用 `AssetResponse`）；`__init__.py` 注册。

- [ ] **Step 4: 跑测试确认 PASS**（4 PASS；JSONB `.as_string()` 在 SQLite fixture 下不可用——若 fixture 是 SQLite，改 `func.json_extract` 兼容写法或 equipment_type 过滤降级为内存过滤，测试不变）

- [ ] **Step 5: 全量回归 + ruff**

- [ ] **Step 6: 提交**

```bash
git add pcs-backend/app/schemas/equip_lib.py pcs-backend/app/services/equip_lib_service.py pcs-backend/app/api/v1/equip_lib.py pcs-backend/app/api/v1/__init__.py pcs-backend/tests/api/v1/test_equip_lib.py
git commit -m "feat(p2-s1.9): equip-lib 沉淀 + 检索（CATEGORY_6 复用 CONFIG 审批链，P2-EQL-001）"
```

---

### Task 1.9.6: 管道等级 Excel 批量导入 + 模板下载

**Files:**
- Modify: `pcs-backend/app/services/pipe_class_service.py`（加 `import_from_excel`）
- Modify: `pcs-backend/app/api/v1/pipe_classes.py`（2 端点）
- Test: `pcs-backend/tests/services/test_pipe_class_import.py`

**Interfaces:**
- Consumes: `openpyxl>=3.1.5`（已在依赖）；Task 1.9.1 `create`
- Produces: `PipeClassService.import_from_excel(session, fileobj) -> dict`（`{imported, skipped, errors[]}`）；端点 `GET /pipe-classes/import-template`（xlsx 下载）、`POST /pipe-classes/import`（multipart）

- [ ] **Step 1: 写失败测试**

```python
# pcs-backend/tests/services/test_pipe_class_import.py
"""Excel 批量导入测试（Task 1.9.6 / P2-STD-001 验收「Excel导入正常」）。"""
import io

from openpyxl import Workbook

from app.services.pipe_class_service import PipeClassService

_HEADERS = [
    "class_id", "class_name", "material_standard", "corrosion_allowance",
    "design_pressure", "design_temperature", "dn_min", "dn_max",
    "sch_series(JSON)", "flange_class", "source", "version",
]
_ROW = ["A1", "等级A1", "GB/T 8163", 1.5, 2.5, 200, 15, 350,
        '{"15": "40", "350": "STD"}', "PN25", "COMPANY_STD", "PC-V1.0"]


def _xlsx(rows: list[list]) -> bytes:
    wb = Workbook(); ws = wb.active; ws.title = "pipe_classes"
    ws.append(_HEADERS)
    for r in rows:
        ws.append(r)
    buf = io.BytesIO(); wb.save(buf); return buf.getvalue()


def _make_create(class_id: str):
    from app.schemas.pipe_class import PipeClassCreate
    return PipeClassCreate(
        class_id=class_id, class_name=f"等级{class_id}", material_standard="GB/T 8163",
        corrosion_allowance=1.5, design_pressure=2.5, design_temperature=200.0,
        dn_series_json={"min": 15, "max": 350},
        sch_series_json={"15": "40", "350": "STD"},
        flange_class="PN25", source="COMPANY_STD", version="PC-V1.0",
    )


async def test_import_two_rows_one_dup(db):
    await PipeClassService.create(db, payload=_make_create("A1"))  # 预存 A1 → 导入行 A1 冲突跳过
    row_a2 = ["A2", "等级A2"] + _ROW[2:]
    result = await PipeClassService.import_from_excel(
        db, io.BytesIO(_xlsx([_ROW, row_a2])))
    assert result["imported"] == 1 and result["skipped"] == 1
    assert await PipeClassService.get(db, "A2")


async def test_import_bad_json_reports_error(db):
    bad = [_ROW[:8] + ["not-json"] + _ROW[9:]]
    result = await PipeClassService.import_from_excel(db, io.BytesIO(_xlsx(bad)))
    assert result["imported"] == 0 and len(result["errors"]) == 1


async def test_template_download(client, sample_pc_token):
    r = await client.get("/api/v1/pipe-classes/import-template",
                         headers={"Authorization": f"Bearer {sample_pc_token}"})
    assert r.status_code == 200
    assert r.headers["content-type"].startswith("application/vnd")
    assert len(r.content) > 500  # 有效 xlsx
```

- [ ] **Step 2: 确认 FAIL**

- [ ] **Step 3: 实现 import_from_excel + 模板端点**

service（追加方法，表头常量与测试 `_HEADERS` 一字不差）：

```python
IMPORT_HEADERS = [
    "class_id", "class_name", "material_standard", "corrosion_allowance",
    "design_pressure", "design_temperature", "dn_min", "dn_max",
    "sch_series(JSON)", "flange_class", "source", "version",
]

@classmethod
def build_import_template(cls) -> bytes:
    import io
    from openpyxl import Workbook
    wb = Workbook(); ws = wb.active; ws.title = "pipe_classes"
    ws.append(IMPORT_HEADERS)
    buf = io.BytesIO(); wb.save(buf); return buf.getvalue()

@classmethod
async def import_from_excel(cls, session, fileobj) -> dict:
    import json
    from openpyxl import load_workbook
    wb = load_workbook(fileobj, read_only=True, data_only=True)
    ws = wb["pipe_classes"] if "pipe_classes" in wb.sheetnames else wb.active
    rows = list(ws.iter_rows(values_only=True))
    if not rows or [str(c) for c in rows[0]] != IMPORT_HEADERS:
        raise PcsError("表头不符（须用 /pipe-classes/import-template 模板）",
                       code="IMPORT_BAD_HEADER", status=422)
    imported = skipped = 0
    errors: list[str] = []
    for i, row in enumerate(rows[1:], start=2):
        if not row or not row[0]:
            continue
        try:
            sch = json.loads(row[8]) if row[8] else {}
            payload = PipeClassCreate(
                class_id=str(row[0]), class_name=str(row[1]), material_standard=str(row[2]),
                corrosion_allowance=float(row[3]), design_pressure=float(row[4]),
                design_temperature=float(row[5]),
                dn_series_json={"min": int(row[6]), "max": int(row[7])},
                sch_series_json={str(k): str(v) for k, v in sch.items()},
                flange_class=str(row[9]), source=str(row[10]), version=str(row[11]),
            )
            await cls.create(session, payload=payload)
            imported += 1
        except PcsError as e:
            if e.code == "PIPE_CLASS_DUP":
                skipped += 1
            else:
                errors.append(f"行{i}: {e}")
        except (ValueError, TypeError, json.JSONDecodeError) as e:
            errors.append(f"行{i}: 解析失败 {e}")
    return {"imported": imported, "skipped": skipped, "errors": errors}
```

router：`GET /pipe-classes/import-template` 返回 `Response(content=..., media_type="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", headers={"Content-Disposition": "attachment; filename=pipe_classes_template.xlsx"})`；`POST /pipe-classes/import` 用 `UploadFile`，调 service 返回结果 dict。注意 FastAPI 路由顺序：`/pipe-classes/import-template` 与 `/pipe-classes/{class_id}` 冲突——静态段路由必须注册在动态段**之前**。

- [ ] **Step 4: 跑测试 PASS**（3 PASS）+ 全量回归 + ruff

- [ ] **Step 5: 真实数据导入验证（P2-OPEN-001 已确认数据源）**

用已入库种子走 service 层导入（两数据源）：
- `app/seeds/pipe_classes_bep_rev0.xlsx`（Worley BEP 4.3 Rev 0，6 等级 COMPANY_STD：A1B/A1E/A2B/G1E/A1F/A2F）
- `app/seeds/pipe_classes_kaimen_20048a.xlsx`（INEOS Kaimen ABS 项目 IFC SPC-0004-C1 + PSI 索引，54 等级 PROJECT：150C10F01RF 等）

```python
# 追加到 tests/services/test_pipe_class_import.py
from pathlib import Path

SEEDS = Path(__file__).parents[2] / "app" / "seeds"

async def test_import_bep_seed(db):
    """P2-OPEN-001：BEP 4.3 Rev0 真实数据全量导入（6 等级，A2F sch 为空 JSON）。"""
    result = await PipeClassService.import_from_excel(
        db, (SEEDS / "pipe_classes_bep_rev0.xlsx").open("rb"))
    assert result["imported"] == 6 and result["skipped"] == 0 and result["errors"] == []
    pc = await PipeClassService.get(db, "G1E")
    assert pc.design_pressure == 10.0 and pc.flange_class == "1500Lb/RJ"

async def test_import_kaimen_seed(db):
    """P2-OPEN-001 第二数据源：Kaimen SPC-0004-C1 54 等级（PROJECT source）。"""
    result = await PipeClassService.import_from_excel(
        db, (SEEDS / "pipe_classes_kaimen_20048a.xlsx").open("rb"))
    assert result["imported"] == 54 and result["errors"] == []
    pc = await PipeClassService.get(db, "150C10F01RF")
    assert pc.source == "PROJECT" and pc.corrosion_allowance == 1.5
    assert pc.sch_series_json["15"] == "SCH 80"
    jacket = await PipeClassService.get(db, "40BA90G12E")  # 夹套芯管
    assert jacket.design_temperature == 350.0 and jacket.design_pressure == 1.6
```

注意：A2F/PSI-only 等级的 sch_series 为 `{}`，`PipeClassCreate.sch_series_json` 需允许空 dict（去掉 `min_length` 类限制或默认空 dict）——若 Step 1 的 schema 不允许，此处一并修。kaimen 种子含 design_pressure 最小 0.01（S01DC2G_R1 常压占位），schema `gt=0` 已兼容。

- [ ] **Step 6: 提交**

```bash
git add pcs-backend/app/services/pipe_class_service.py pcs-backend/app/api/v1/pipe_classes.py pcs-backend/tests/services/test_pipe_class_import.py
git commit -m "feat(p2-s1.9): 管道等级 Excel 批量导入 + 模板下载 + BEP Rev0 种子导入验证（P2-STD-001/P2-OPEN-001 收口）"
```

---

# 不在本计划范围（明确排除）

| 项 | 理由 | 归属 |
|---|---|---|
| Vc/Zc 关联式 | vendored 库与文献常数均无法可靠查证；未验证工程常数不入库（seed 已标 `out_of_scope`） | P3 PETROLEUM 向导随 SUP 补 |
| CoolProp / ht 接线 | CoolProp 源码需 cmake 编译（非 path 依赖可装）；水蒸气需求 chemicals.iapws 已覆盖；ht 属 P4 管网计算 | 确需时（pypi wheel） |
| equip-lib 相似度计算 / settle pending 列表端点 | P7 §3.2.3 范围（GT /equip-lib/settle/pending） | P7 |
| ProjectTemplate.default_config_json 增「默认管道等级列表」 | CATEGORY_1 项目模板域（spec §3.2.7），非本次排除表项 | Sprint 1.12 / P3 |
| PUBLISH 端点为 CATEGORY_6 加额外门禁 | 复用既有 5 态审批链已满足「审核审批→入库生效」 | — |
| 前端 pipe-class / equip-lib 管理页 | P2 后端先行；前端统一在 P2 尾段 | P2 Sprint 2 |

---

# 自检（spec 对照）

- [x] **Spec coverage**：§3.2.5 五端点 → Task 1.9.2；CRUD/项目分配/在用不可删 → 1.9.1/1.9.2；Excel 导入+模板 → 1.9.6；§3.2.6 沉淀流程/标准化校验/审批后入库 → 1.9.5；§3.2.4 V1.2 三行系数类型 → 1.9.3/1.9.4；ADR-0019 虚拟组分 25°C 切割 → 1.9.3；§3.2.5 覆盖范围「chemicals 库物性数据」+ ADR-0019 chemicals/thermo 估算 → 1.9.0 接线。
- [x] **Placeholder scan**：无 TBD/TODO/“similar to”；两处标注「执行时照抄既有文件/fixture」是防错指令而非占位。
- [x] **Type consistency**：`PipeClassCreate/Update/Response`、`ProjectAssignRequest`、`EquipLibSettleRequest`、service 方法签名在 Task 间引用一致；`RD80_CONSTANTS` 在 1.9.3 与 1.9.4 数值一致；`estimate` 返回键 `nu_20c_cst` 在 1.9.3 测试/实现与切割展开一致。

---

# 执行交接

**Plan complete and saved to `docs/superpowers/plans/2026-09-04-p2-sprint-1.9.md`.**

**Two execution options:**

1. **Subagent-Driven (recommended)** — fresh subagent per task，每 task 独立 review（spec compliance + code quality），7 task 预计 1–2 人天
2. **Inline Execution** — 当前会话按 checkpoint 批执行

**Which approach?**

---

## 尚未解决的问题

1. ~~P2-OPEN-001~~ **已解决（2026-09-05）**：用户提供 Worley BEP 4.3 Rev 0，已提取 6 等级种子（`app/seeds/pipe_classes_bep_rev0.{json,xlsx}`），Task 1.9.6 Step 5 验证导入
2. **equip-lib settle 的「待审批列表」端点**（P7 spec `GET /equip-lib/settle/pending`）本 sprint 不做，但 P2 验收「沉淀申请流程正常」若验收人要求看列表，需临时用 `/config/assets?category=CATEGORY_6&status=PENDING` 顶替（现有端点是否支持 category 过滤未核实）
3. **JSONB `content_json["equipment_type"].as_string()` 在 SQLite 测试 fixture 下的兼容性**——Task 1.9.5 Step 4 已注降级方案，若实际不兼容需定夺用 `json_extract` 还是内存过滤
4. **`make_asset` fixture 是否已支持 `name` 参数**——1.9.5 测试假设可传 name；不支持则需扩 fixture（已注明「扩展而非绕过」）
5. **BEP 种子两处忠实性保留**：A1F 文档含 `315/0.3` 设计点（疑为 31.5°C 之误，CPVC 不耐 315°C），种子按保守 50°C 上限；A2F 常压重力流 design_pressure 取 0.1MPa 占位——均标注在 JSON `note` 字段，工艺确认后改
