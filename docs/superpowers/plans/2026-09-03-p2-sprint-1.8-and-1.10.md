# P2 Sprint 1.8 闭环 + Sprint 1.10 V1.4 收敛实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完全弥补 P2 V1.4 偏差审计（commit `35a1614`）识别的 P0/P1 缺口：preconditions 接入、PcsError 统一、CATEGORY_3 默认 seed、default_config_json schema 验证（Sprint 1.8）；折标煤系数组、DETAIL/HTRI 模板入库、ADR-0029、DICT V3.9 同步（Sprint 1.10）。

**Architecture:**
- **Sprint 1.8** 走"最小侵入"：复用现有 `FormulaEngine` AST 解析路径，新增 `parse_preconditions` 入口；`FormulaSecurityError` / `PreconditionViolation` 统一继承 `PcsError`；`CoefficientService` 加幂等 seed 钩子；新 Pydantic v2 子模型在 `ProjectTemplateService.load` 时 `parse_obj` 验证。
- **Sprint 1.10** 走"V1.4 一次性收敛"：新建 `pcs_toe_conversion_factors` 表 + `htri_template_schemas` 表；扩展 `TemplateFile.template_version_seq`；通过 Alembic 迁移落地，ADR-0029 起草后评审。

**Tech Stack:** FastAPI + SQLAlchemy 2.0 + Pydantic v2 + Alembic + SymPy/AST + pytest + JSONB。

**Spec:**
- `/home/pangzy/code_project/PCS/spec/工艺专用综合计算软件需求规格说明书 Web版 P2.md` V1.4
- `/home/pangzy/code_project/PCS/spec/PCS 本体论与语义关系研究说明（V1.6）.md` §2.3 preconditions 表达力边界
- `/home/pangzy/code_project/PCS/spec/PCS-REQ-2026-002-SUP-008PCS 数据模型对齐与 Excel 导入增补规格说明书.md` V1.1 §2.5 auxiliary_consumption
- `/home/pangzy/code_project/PCS/spec/PCS-REQ-2026-002-SUP-010汽包蒸汽发生器与综合能耗增补规格说明书.md` V1.1 §3.3.4 utility_energy_summary

## Global Constraints

- **Pydantic ↔ ORM CI 校验**：本体论 V1.6 §6 规则 11——新增/修改 Pydantic 模型必须同步更新 SQLAlchemy 模型，由 CI 比对字段（pcs-backend/app/schemas/ ↔ pcs-backend/app/models/）。
- **9 态门禁 + record_hash + 无 version 字段**（SUP-007 §10）：本计划不涉及记录层门禁；新建 `pcs_toe_conversion_factors` / `htri_template_schemas` 走 CONFIG 配置资产 5 态（DRAFT→PENDING→APPROVED→PUBLISHED→OBSOLETE），不入记录层。
- **REJECT-only**：preconditions 违规统一抛 `PcsError`（`code="PRECONDITION_VIOLATION"`，status=422），不静默吞错。
- **TDD 强制**：每个 Task 的 step 1-2 写失败测试 → step 3-4 实现至绿 → step 5 commit。
- **commit 粒度**：每 Task 独立 commit（docs/feat/test 分明），不允许 squash。
- **测试覆盖率**：本计划所有 service 新增/修改行必须 ≥ 80% 覆盖（本体论 V1.6 §6 规则 12）。

---

## 文件结构（新增/修改）

| 类型 | 文件 | 内容 |
|---|---|---|
| New | `pcs-backend/app/schemas/project_template.py` | Pydantic v2 子模型 7 个（Task 1.8.4） |
| New | `pcs-backend/app/services/project_template_service.py` | ProjectTemplate 加载 schema 验证（Task 1.8.4） |
| New | `pcs-backend/app/services/toe_conversion_service.py` | 折标煤系 CRUD + 按 fuel_type/year 查询（Task 1.10.1） |
| New | `pcs-backend/app/models/htri_template.py` | HtriTemplateSchema 模型（Task 1.10.3） |
| New | `pcs-backend/app/seeds/category3_defaults.json` | 6 张 CATEGORY_3 默认表 seed（Task 1.8.3） |
| New | `pcs-backend/app/seeds/templates/detail_121_a_101.json` | DETAIL 模板元数据（Task 1.10.2） |
| New | `pcs-backend/app/seeds/templates/detail_131_e_102_eor.json` | DETAIL 模板元数据（Task 1.10.2） |
| New | `pcs-backend/alembic/versions/XXXX_add_toe_conversion.py` | 折标煤迁移（Task 1.10.1） |
| New | `pcs-backend/alembic/versions/XXXX_add_htri_template_schemas.py` | HTRI schema 迁移（Task 1.10.3） |
| New | `docs/adr/0029-toe-conversion-and-detail-htri-templates.md` | ADR（Task 1.10.4） |
| Modify | `pcs-backend/app/services/formula_engine.py:23-122` | `FormulaSecurityError → PcsError` + `parse_preconditions` + `evaluate_preconditions`（Task 1.8.1+1.8.2） |
| Modify | `pcs-backend/app/services/formula_service.py:39-69` | `run_unit_tests` 钩入 preconditions（Task 1.8.1） |
| Modify | `pcs-backend/app/services/exceptions.py` | 新增 `PreconditionViolation(PcsError)`（Task 1.8.1） |
| Modify | `pcs-backend/app/services/coefficient_service.py` | `seed_default_tables()` 幂等（Task 1.8.3） |
| Modify | `pcs-backend/app/main.py` lifespan | seed 钩子 + 启动校验（Task 1.8.3） |
| Modify | `pcs-backend/app/models/config_domain.py` | `ToeConversionFactor` 新表（Task 1.10.1）+ `TemplateFile.template_version_seq`（Task 1.10.2） |
| Modify | `pcs-backend/app/services/template_service.py:38-90` | `upload` 自动续 seq + `register_htri_schema`（Task 1.10.2+1.10.3） |
| Modify | `pcs-backend/app/api/v1/config.py` | 新增 `/config/toe-conversion` 端点（Task 1.10.1） |
| Modify | `docs/dict/DICT-ALL-003.md` | V3.8 → V3.9（Task 1.10.4） |
| Test | `pcs-backend/tests/services/test_formula_engine_preconditions.py` | 8 类 preconditions 单测（Task 1.8.1） |
| Test | `pcs-backend/tests/services/test_toe_conversion_service.py` | CRUD + 按 fuel_type/year 查询（Task 1.10.1） |
| Test | `pcs-backend/tests/seeds/test_category3_seeds.py` | 6 张表 seed 幂等（Task 1.8.3） |
| Test | `pcs-backend/tests/seeds/test_detail_templates.py` | DETAIL 模板元数据（Task 1.10.2） |
| Test | `pcs-backend/tests/schemas/test_project_template_schema.py` | 7 子配置 Pydantic 验证（Task 1.8.4） |
| Test | `pcs-backend/tests/models/test_htri_template_schema.py` | HTRI 33/23 列 schema（Task 1.10.3） |

---

# Sprint 1.8 闭环

## Task 1.8.1: preconditions 字段全链路接入

**Files:**
- Modify: `pcs-backend/app/services/formula_engine.py:23-122`
- Modify: `pcs-backend/app/services/formula_service.py:39-69`
- New: `pcs-backend/app/services/exceptions.py`（如不存在则建空 `__init__.py`）
- Test: `pcs-backend/tests/services/test_formula_engine_preconditions.py`

**Interfaces:**
- Consumes: `content_json["preconditions"]`（list of `{id, description, target, expression, when}`），`content_json["parameters"]`
- Produces: `FormulaEngine.evaluate_preconditions(preconditions: list, context: dict) -> list[str]`（返回违规 ID 列表，pre 与 post 阶段均走同一入口）；`PreconditionViolation(PcsError)` 异常

- [ ] **Step 1: 写失败测试（8 类）**

```python
# pcs-backend/tests/services/test_formula_engine_preconditions.py
import pytest
from app.services.formula_engine import FormulaEngine
from app.services.exceptions import PreconditionViolation

def test_preconditions_input_required():
    """input.T 必须存在于 context，否则 REJECT"""
    preconds = [{"id": "PRE-001", "target": "input.T", "expression": "T > 0"}]
    with pytest.raises(PreconditionViolation):
        FormulaEngine.evaluate_preconditions(preconds, {"input": {}})

def test_preconditions_params_required():
    """params.K 必须存在于 context，否则 REJECT"""
    preconds = [{"id": "PRE-002", "target": "params.K", "expression": "K > 0"}]
    with pytest.raises(PreconditionViolation):
        FormulaEngine.evaluate_preconditions(preconds, {"params": {}})

def test_preconditions_result_forbidden_pre():
    """pre 阶段 result.* 必须不存在，否则 REJECT"""
    preconds = [{"id": "PRE-003", "target": "result.x", "expression": "x > 0"}]
    with pytest.raises(PreconditionViolation):
        FormulaEngine.evaluate_preconditions(preconds, {}, phase="pre")

def test_preconditions_context_forbidden():
    """context.* 引用一律 REJECT（V1.6 §2.3 边界）"""
    preconds = [{"id": "PRE-004", "target": "context.db", "expression": "db is not None"}]
    with pytest.raises(PreconditionViolation):
        FormulaEngine.evaluate_preconditions(preconds, {"context": {"db": "x"}})

def test_preconditions_constant_trace_fail():
    """params.* 必须能溯源到 CoefficientTables，否则 REJECT"""
    preconds = [{"id": "PRE-005", "target": "params.K", "expression": "K > 0"}]
    with pytest.raises(PreconditionViolation):
        FormulaEngine.evaluate_preconditions(
            preconds, {"params": {"K": 1.0}}, trace_table={"K": None}  # 溯源失败
        )

def test_preconditions_pre_violation():
    """pre 阶段违规抛 PreconditionViolation"""
    preconds = [{"id": "PRE-006", "target": "input.T", "expression": "T > 100"}]
    with pytest.raises(PreconditionViolation):
        FormulaEngine.evaluate_preconditions(preconds, {"input": {"T": 50}}, phase="pre")

def test_preconditions_post_check():
    """post 阶段 result.* 可访问且应满足 expression"""
    preconds = [{"id": "PRE-007", "target": "result.q", "expression": "q > 0"}]
    FormulaEngine.evaluate_preconditions(
        preconds, {"result": {"q": 1.5}}, phase="post"
    )  # 不抛错

def test_preconditions_nested_reference():
    """嵌套引用 input.subfield.field 应正确解析"""
    preconds = [{"id": "PRE-008", "target": "input.stream.temperature", "expression": "temperature > 0"}]
    FormulaEngine.evaluate_preconditions(
        preconds, {"input": {"stream": {"temperature": 100}}}, phase="pre"
    )  # 不抛错
```

- [ ] **Step 2: 运行测试确认全部 FAIL**

Run: `cd pcs-backend && pytest tests/services/test_formula_engine_preconditions.py -v`
Expected: 8 failures, "PreconditionViolation not defined" / "evaluate_preconditions not defined"

- [ ] **Step 3: 实现 PreconditionViolation + evaluate_preconditions**

```python
# pcs-backend/app/services/exceptions.py
from typing import Any

class PcsError(Exception):
    """PCS 统一异常基类。所有 service 层异常应继承此类。"""
    code: str = "PCS_ERROR"
    status: int = 422

    def __init__(self, message: str, *, code: str | None = None, status: int | None = None,
                 details: dict[str, Any] | None = None):
        super().__init__(message)
        if code is not None:
            self.code = code
        if status is not None:
            self.status = status
        self.details = details or {}

class PreconditionViolation(PcsError):
    """公式前置/后置条件违规。code=PRECONDITION_VIOLATION, status=422, REJECT-only。"""
    code = "PRECONDITION_VIOLATION"
    status = 422
```

```python
# pcs-backend/app/services/formula_engine.py 新增方法
@classmethod
def evaluate_preconditions(
    cls, preconditions: list[dict], context: dict, *, phase: str = "pre",
    trace_table: dict[str, str | None] | None = None,
) -> list[str]:
    """评估 preconditions 数组，返回违规 ID 列表。

    phase="pre"：input.*/params.* 可用；result.* 不应存在
    phase="post"：result.* 可用；input.*/params.* 只读

    变量白名单：input.* / params.* / result.* ；
    context.* 引用一律 REJECT（V1.6 §2.3 表达力边界）。

    trace_table：params.* → CoefficientTable 溯源映射；
                 缺键或 None 值 → REJECT。
    """
    violations: list[str] = []
    for pc in preconditions:
        target = pc.get("target", "")
        if not target.startswith(("input.", "params.", "result.")):
            raise PreconditionViolation(
                f"precondition target 必须是 input.*/params.*/result.*；得到 {target!r}",
                details={"id": pc.get("id"), "target": target},
            )

        parts = target.split(".", 1)
        namespace, path = parts[0], parts[1]

        if namespace == "context":
            raise PreconditionViolation(
                f"禁止引用 context.*（V1.6 §2.3 边界）；precondition id={pc.get('id')!r}",
                details={"id": pc.get("id"), "target": target},
            )

        if phase == "pre" and namespace == "result":
            raise PreconditionViolation(
                f"pre 阶段禁止引用 result.*；precondition id={pc.get('id')!r}",
                details={"id": pc.get("id"), "target": target},
            )

        # 解析嵌套值
        value = context.get(namespace, {})
        for segment in path.split("."):
            if not isinstance(value, dict) or segment not in value:
                raise PreconditionViolation(
                    f"target 路径解析失败：{target!r}",
                    details={"id": pc.get("id"), "target": target, "missing": segment},
                )
            value = value[segment]

        # 溯源校验
        if namespace == "params" and trace_table is not None:
            if trace_table.get(path) is None:
                raise PreconditionViolation(
                    f"params.{path} 未溯源到 CoefficientTables",
                    details={"id": pc.get("id"), "target": target, "param": path},
                )

        # 评估 expression（复用 AST 白名单）
        try:
            tree = ast.parse(pc["expression"], mode="eval")
            cls._validate_ast(tree)
            code = compile(tree, "<precondition>", "eval")
            namespace_ns = cls._build_namespace()
            namespace_ns.update({"value": value})
            result = eval(code, {"__builtins__": {}}, namespace_ns)  # noqa: S307
            if not result:
                violations.append(pc.get("id", target))
        except FormulaSecurityError as e:
            raise PreconditionViolation(
                f"precondition expression 含禁止 AST 节点：{e}",
                details={"id": pc.get("id"), "target": target},
            ) from e

    if violations:
        raise PreconditionViolation(
            f"preconditions 违规：{violations}",
            details={"violations": violations},
        )
    return violations
```

- [ ] **Step 4: 接入 formula_service run_unit_tests**

```python
# pcs-backend/app/services/formula_service.py:39-69 修改 run_unit_tests
@classmethod
def run_unit_tests_with_preconditions(
    cls, expression: str, parameters: dict, unit_tests: list[dict],
    preconditions: list[dict] | None = None,
    trace_table: dict[str, str | None] | None = None,
) -> tuple[int, int]:
    """扩展 run_unit_tests：先 evaluate_preconditions(pre)，再 unit_test，最后 evaluate_preconditions(post)。"""
    if preconditions:
        # pre 阶段：仅校验 input.*/params.*
        FormulaEngine.evaluate_preconditions(
            preconditions, {"input": {}, "params": parameters},
            phase="pre", trace_table=trace_table,
        )
    fn = FormulaEngine.parse(expression, parameters)
    passed = 0
    results_for_post: list[dict] = []
    for tc in unit_tests:
        try:
            result = fn(tc["params"])
            if abs(result - tc["expected"]) <= tc.get("tolerance", 0.01):
                passed += 1
            results_for_post.append({"result": result, "params": tc["params"]})
        except Exception:
            pass
    if preconditions:
        # post 阶段：校验 result.*
        for ctx in results_for_post:
            FormulaEngine.evaluate_preconditions(
                preconditions, ctx, phase="post",
            )
    return passed, len(unit_tests)
```

- [ ] **Step 5: 运行测试确认全部 PASS**

Run: `cd pcs-backend && pytest tests/services/test_formula_engine_preconditions.py -v`
Expected: 8 PASS

- [ ] **Step 6: 提交**

```bash
git add pcs-backend/app/services/exceptions.py \
        pcs-backend/app/services/formula_engine.py \
        pcs-backend/app/services/formula_service.py \
        pcs-backend/tests/services/test_formula_engine_preconditions.py
git commit -m "feat(p2-s1.8): preconditions 全链路接入 + PreconditionViolation(PcsError)"
```

---

## Task 1.8.2: FormulaSecurityError → PcsError 统一

**Files:**
- Modify: `pcs-backend/app/services/formula_engine.py:23`

**Interfaces:**
- Consumes: 现有 `FormulaSecurityError`（`formula_engine.py:23`）
- Produces: `FormulaSecurityError(PcsError)`，`code="FORMULA_SECURITY"`，`status=422`

- [ ] **Step 1: 写失败测试**

```python
# pcs-backend/tests/services/test_formula_security_error.py
from app.services.formula_engine import FormulaEngine, FormulaSecurityError
from app.services.exceptions import PcsError

def test_formula_security_error_inherits_pcs_error():
    assert issubclass(FormulaSecurityError, PcsError)

def test_formula_security_error_code_and_status():
    e = FormulaSecurityError("test")
    assert e.code == "FORMULA_SECURITY"
    assert e.status == 422
```

- [ ] **Step 2: 运行测试确认 FAIL**

Run: `cd pcs-backend && pytest tests/services/test_formula_security_error.py -v`
Expected: 2 failures

- [ ] **Step 3: 实现继承**

```python
# pcs-backend/app/services/formula_engine.py:23 修改
from app.services.exceptions import PcsError

class FormulaSecurityError(PcsError):
    """公式包含禁止的 AST 节点或名称时抛出。code=FORMULA_SECURITY, status=422."""
    code = "FORMULA_SECURITY"
    status = 422
```

- [ ] **Step 4: 运行测试确认 PASS**

Run: `cd pcs-backend && pytest tests/services/test_formula_security_error.py -v`
Expected: 2 PASS

- [ ] **Step 5: 跑全量公式测试确认无回归**

Run: `cd pcs-backend && pytest tests/services/test_formula_engine.py tests/services/test_formula_service.py -v`
Expected: 所有现有测试 PASS（FormulaSecurityError 行为兼容）

- [ ] **Step 6: 提交**

```bash
git add pcs-backend/app/services/formula_engine.py pcs-backend/tests/services/test_formula_security_error.py
git commit -m "refactor(p2-s1.8): FormulaSecurityError 继承 PcsError"
```

---

## Task 1.8.3: CATEGORY_3 默认表 seed（V1.0 6 张）

**Files:**
- New: `pcs-backend/app/seeds/category3_defaults.json`
- Modify: `pcs-backend/app/services/coefficient_service.py`（追加 async `seed_default_tables` classmethod）
- Modify: `pcs-backend/app/main.py:20-36`（在现有 lifespan 内**追加** seed 调用，不替换）
- Test: `pcs-backend/tests/seeds/test_category3_seeds.py`

**Interfaces:**
- Consumes: `category3_defaults.json`（6 张表 schema + seed rows）
- Produces: `CoefficientService.seed_default_tables(session)` async classmethod；启动时调用一次

> **R7 (reconcile with existing async pattern)**: 现有 `coefficient_service.py` 全部方法用 `AsyncSession` + `await self.session.flush()`；`db/session.py` 只暴露 `get_async_session_factory()`，无 sync `SessionLocal`。计划 sync 版本不兼容。实施时使用 async session factory + direct `session.add(CoefficientTable(...))` 插入（`asset_id=None`，因 seed 表无业务资产归属）。

- [ ] **Step 1: 写失败测试**

```python
# pcs-backend/tests/seeds/test_category3_seeds.py
import pytest
from app.services.coefficient_service import CoefficientService
from app.db.session import get_async_session_factory

EXPECTED_TABLES = [
    "souders_brown_k",
    "recommended_velocity",
    "pump_power_safety_factor",
    "pipe_roughness_default",
    "heat_exchanger_weight_factor",
    "fitting_resistance_mapping",
]

@pytest.mark.asyncio
async def test_seed_default_tables_creates_six():
    factory = get_async_session_factory()
    async with factory() as session:
        created = await CoefficientService.seed_default_tables(session)
    assert len(created) == 6
    for name in EXPECTED_TABLES:
        assert name in created

@pytest.mark.asyncio
async def test_seed_default_tables_idempotent():
    factory = get_async_session_factory()
    async with factory() as session:
        await CoefficientService.seed_default_tables(session)
        second = await CoefficientService.seed_default_tables(session)
    assert second == []  # 第二次幂等无新增

@pytest.mark.asyncio
@pytest.mark.parametrize("table_name", EXPECTED_TABLES)
async def test_seed_table_queryable(table_name):
    factory = get_async_session_factory()
    async with factory() as session:
        await CoefficientService.seed_default_tables(session)
        # 用 session.get + 名称过滤查行（实际为查询逻辑，参见 coefficient_service.query）
        from sqlalchemy import select
        from app.models.config_domain import CoefficientTable
        result = await session.execute(
            select(CoefficientTable).where(CoefficientTable.name == table_name)
        )
        row = result.scalar_one()
    assert row.applicable_range
    assert len(row.data_json["rows"]) >= 1
```

- [ ] **Step 2: 运行测试确认 FAIL**

Run: `cd pcs-backend && pytest tests/seeds/test_category3_seeds.py -v`
Expected: failures, "seed_default_tables not implemented"

- [ ] **Step 3: 写 seed JSON**

```json
// pcs-backend/app/seeds/category3_defaults.json
{
  "souders_brown_k": {
    "applicable_range": "气液分离器（VESSEL 模块）",
    "std_source": "附录F / Perry's Handbook 8ed Table 14-23",
    "rows": [
      {"equipment_type": "立式分离器", "with_mist_eliminator": true,  "K_min": 0.06, "K_max": 0.16, "K_default": 0.11},
      {"equipment_type": "立式分离器", "with_mist_eliminator": false, "K_min": 0.03, "K_max": 0.06, "K_default": 0.045},
      {"equipment_type": "卧式分离器", "with_mist_eliminator": true,  "K_min": 0.10, "K_max": 0.30, "K_default": 0.20},
      {"equipment_type": "分液罐",     "with_mist_eliminator": false, "K_min": 0.02, "K_max": 0.06, "K_default": 0.04}
    ]
  },
  "recommended_velocity": {
    "applicable_range": "管道推荐流速（PIPE 模块，HG/T 20570.6-95）",
    "std_source": "HG/T 20570.6-95 表 5.1",
    "rows": [
      {"service": "一般液体",       "velocity_min": 0.5, "velocity_max": 2.5, "unit": "m/s"},
      {"service": "粘稠液体",       "velocity_min": 0.5, "velocity_max": 1.5, "unit": "m/s"},
      {"service": "饱和蒸汽",       "velocity_min": 20,  "velocity_max": 40,  "unit": "m/s"},
      {"service": "过热蒸汽",       "velocity_min": 30,  "velocity_max": 60,  "unit": "m/s"},
      {"service": "一般气体",       "velocity_min": 10,  "velocity_max": 30,  "unit": "m/s"},
      {"service": "两相流（气液）", "velocity_min": 3,   "velocity_max": 15,  "unit": "m/s"}
    ]
  },
  "pump_power_safety_factor": {
    "applicable_range": "泵功率安全系数（PUMP 模块，API 610）",
    "std_source": "API 610 11ed Annex C",
    "rows": [
      {"service": "一般离心泵",       "safety_factor": 1.10},
      {"service": "高温油泵",         "safety_factor": 1.15},
      {"service": "高粘度泵",         "safety_factor": 1.20},
      {"service": "立式泵",           "safety_factor": 1.10},
      {"service": "锅炉给水泵",       "safety_factor": 1.15}
    ]
  },
  "pipe_roughness_default": {
    "applicable_range": "管道粗糙度默认值（PIPE 模块）",
    "std_source": "工程经验 / Crane TP-410",
    "rows": [
      {"material": "碳钢（新管）",     "roughness_mm": 0.045},
      {"material": "碳钢（使用中）",   "roughness_mm": 0.20},
      {"material": "不锈钢",           "roughness_mm": 0.015},
      {"material": "合金钢",           "roughness_mm": 0.045},
      {"material": "铜/铜合金",        "roughness_mm": 0.0015},
      {"material": "玻璃/陶瓷衬里",   "roughness_mm": 0.0015}
    ]
  },
  "heat_exchanger_weight_factor": {
    "applicable_range": "换热器重量估算系数（HEAT 模块）",
    "std_source": "工程经验 / GPSA 12ed Sec 13",
    "rows": [
      {"type": "BEM", "factor_per_m2": 0.045, "unit": "ton/m²"},
      {"type": "AES", "factor_per_m2": 0.040, "unit": "ton/m²"},
      {"type": "AKT", "factor_per_m2": 0.060, "unit": "ton/m²"},
      {"type": "ACHE", "factor_per_m2": 0.035, "unit": "ton/m²"}
    ]
  },
  "fitting_resistance_mapping": {
    "applicable_range": "管件阻力系数映射表（PIPE 模块，fluids.fittings）",
    "std_source": "fluids.fittings / Crane TP-410",
    "rows": [
      {"fitting_type": "elbow_90_long",   "K": 0.2,  "L_over_D": 30},
      {"fitting_type": "elbow_90_short",  "K": 0.9,  "L_over_D": 0},
      {"fitting_type": "elbow_45_long",   "K": 0.4,  "L_over_D": 16},
      {"fitting_type": "tee_line",        "K": 0.4,  "L_over_D": 20},
      {"fitting_type": "tee_branch",      "K": 1.0,  "L_over_D": 60},
      {"fitting_type": "valve_gate_full", "K": 0.15, "L_over_D": 8},
      {"fitting_type": "valve_globe",     "K": 6.0,  "L_over_D": 340},
      {"fitting_type": "valve_check",     "K": 2.5,  "L_over_D": 140}
    ]
  }
}
```

- [ ] **Step 4: 实现 async seed_default_tables**

```python
# pcs-backend/app/services/coefficient_service.py 新增 async classmethod
import json
from pathlib import Path
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

class CoefficientService:
    # ... 既有方法保持不变 ...

    @classmethod
    async def seed_default_tables(cls, session: AsyncSession) -> list[str]:
        """幂等 seed：CATEGORY_3 默认 6 张表。仅在未 seed 时插入。

        使用 direct ORM add 而非 cls.create_table：seed 表无业务 asset 归属，
        asset_id 传 None（model 字段已 nullable）。
        """
        seed_file = Path(__file__).parent.parent / "seeds" / "category3_defaults.json"
        defaults = json.loads(seed_file.read_text(encoding="utf-8"))
        created: list[str] = []
        for table_name, payload in defaults.items():
            # 幂等：先查是否已有同名 SEED-V1.0 行
            result = await session.execute(
                select(CoefficientTable).where(
                    CoefficientTable.name == table_name,
                    CoefficientTable.version == "SEED-V1.0",
                )
            )
            if result.scalar_one_or_none():
                continue
            session.add(CoefficientTable(
                asset_id=None,  # seed 表无业务资产归属
                name=table_name,
                applicable_range=payload["applicable_range"],
                std_source=payload["std_source"],
                data_json={"rows": payload["rows"]},
                version="SEED-V1.0",
                status="PUBLISHED",  # 默认 seed 直接发布
            ))
            created.append(table_name)
        if created:
            await session.commit()
        return created
```

- [ ] **Step 5: 启动钩子（在现有 lifespan 内追加，不替换）**

```python
# pcs-backend/app/main.py 修改现有 lifespan（不替换整个函数）
# 1) 在 imports 区域追加：
from app.services.coefficient_service import CoefficientService
from app.db.session import get_async_session_factory

# 2) 在 lifespan 内、yield 之前、await check_database_async() 之后追加：
@asynccontextmanager
async def lifespan(app: FastAPI) -> AsyncIterator[None]:
    settings = get_settings()
    assert_secret_key_configured()
    if settings.is_production:
        for route in app.routes:
            if getattr(route, "path", "").endswith("/mock-login"):
                raise RuntimeError("mock-login route MUST NOT be mounted in production")
    await check_database_async()
    get_async_engine()

    # V1.4 P2-OPEN-005 / SPEC-P2 §3.2.3：启动时 seed CATEGORY_3 默认 6 张系数表（幂等）
    factory = get_async_session_factory()
    async with factory() as session:
        await CoefficientService.seed_default_tables(session)

    try:
        yield
    finally:
        await dispose_engines_async()
```

- [ ] **Step 6: 运行测试确认 PASS**

Run: `cd pcs-backend && pytest tests/seeds/test_category3_seeds.py -v`
Expected: 所有 PASS（含 parametrize）

- [ ] **Step 7: 提交**

```bash
git add pcs-backend/app/seeds/category3_defaults.json \
        pcs-backend/app/services/coefficient_service.py \
        pcs-backend/app/main.py \
        pcs-backend/tests/seeds/test_category3_seeds.py
git commit -m "feat(p2-s1.8): CATEGORY_3 默认 6 张系数表 seed + 启动幂等钩子"
```

---

## Task 1.8.4: default_config_json schema 验证（V1.1 SUP-007）

**Files:**
- New: `pcs-backend/app/schemas/project_template.py`
- New: `pcs-backend/app/services/project_template_service.py`
- Test: `pcs-backend/tests/schemas/test_project_template_schema.py`

**Interfaces:**
- Consumes: `ProjectTemplate.default_config_json: dict`
- Produces: `ProjectTemplateService.load(template_id)` → 验证后返回；若 `parse_obj` 失败抛 `PcsError`（`code="TEMPLATE_CONFIG_INVALID"`）

- [ ] **Step 1: 写失败测试（7 子配置 + 错误路径）**

```python
# pcs-backend/tests/schemas/test_project_template_schema.py
import pytest
from app.schemas.project_template import (
    ProjectTemplateConfig, RecordApprovalConfig, StreamApprovalConfig,
    NumberingConfig, CustomerApprovalConfig, SignatureMatrixBinding,
    VersionSequenceConfig, ReversalRoleConfig,
)

def test_record_approval_config_valid():
    cfg = RecordApprovalConfig(steps=[{"role": "审核", "level": 1}, {"role": "审定", "level": 2}])
    assert cfg.steps[1].level == 2

def test_stream_approval_config_levels_1_to_2():
    cfg = StreamApprovalConfig(max_depth=2)
    assert cfg.max_depth == 2
    with pytest.raises(Exception):
        StreamApprovalConfig(max_depth=3)  # 越界

def test_numbering_config_segments():
    cfg = NumberingConfig(segments=[{"type": "PREFIX", "value": "PRJ"}, {"type": "SEQ"}], separator="-")
    assert len(cfg.segments) == 2

def test_customer_approval_config_attachment_required():
    cfg = CustomerApprovalConfig(proxy_allowed=True, attachment_required=True)
    assert cfg.proxy_allowed

def test_signature_matrix_binding_steps():
    cfg = SignatureMatrixBinding(matrix_name="DEFAULT_2_LEVEL", steps=[{"role": "DESIGNER"}, {"role": "REVIEWER"}])
    assert cfg.steps[1].role == "REVIEWER"

def test_version_sequence_config_skip_alpha():
    cfg = VersionSequenceConfig(skip_alpha_versions=True, allowed_purposes=["DRAFT", "PUBLISHED"])
    assert cfg.skip_alpha_versions

def test_reversal_role_config():
    cfg = ReversalRoleConfig(reversal_approver_role="REVIEWER")
    assert cfg.reversal_approver_role == "REVIEWER"

def test_full_project_template_config_loads():
    cfg = ProjectTemplateConfig(
        record_approval=RecordApprovalConfig(steps=[{"role": "审核", "level": 1}]),
        stream_approval=StreamApprovalConfig(max_depth=1),
        numbering=NumberingConfig(segments=[{"type": "SEQ"}], separator="-"),
        customer_approval=CustomerApprovalConfig(proxy_allowed=False),
        signature_matrix=SignatureMatrixBinding(matrix_name="DEFAULT_1_LEVEL", steps=[{"role": "DESIGNER"}]),
        version_sequence=VersionSequenceConfig(allowed_purposes=["DRAFT"]),
        reversal_role=ReversalRoleConfig(reversal_approver_role="DESIGNER"),
    )
    assert cfg.record_approval.steps[0].role == "审核"

def test_invalid_config_raises_pcs_error():
    from app.services.exceptions import PcsError
    from app.services.project_template_service import ProjectTemplateService
    with pytest.raises(PcsError) as exc_info:
        ProjectTemplateService.validate_config({
            "stream_approval": {"max_depth": 5},  # 越界
        })
    assert exc_info.value.code == "TEMPLATE_CONFIG_INVALID"
```

- [ ] **Step 2: 运行测试确认 FAIL**

Run: `cd pcs-backend && pytest tests/schemas/test_project_template_schema.py -v`
Expected: failures, "schemas/project_template.py not found"

- [ ] **Step 3: 实现 Pydantic v2 子模型**

```python
# pcs-backend/app/schemas/project_template.py
from __future__ import annotations
from pydantic import BaseModel, Field, field_validator
from typing import Literal

class ApprovalStep(BaseModel):
    role: str = Field(..., min_length=1)
    level: int | None = None
    required: bool = True
    can_self_check: bool = False
    can_skip: bool = False

class RecordApprovalConfig(BaseModel):
    steps: list[ApprovalStep] = Field(..., min_length=1, max_length=4)

class StreamApprovalConfig(BaseModel):
    max_depth: Literal[1, 2] = 2

class NumberingSegment(BaseModel):
    type: Literal["PREFIX", "PROJECT_CODE", "SEQ", "SUFFIX", "YEAR", "CATEGORY"]
    value: str | None = None

class NumberingConfig(BaseModel):
    segments: list[NumberingSegment] = Field(..., min_length=1)
    separator: str = Field(default="-", max_length=5)
    revision_separate: bool = True

class CustomerApprovalConfig(BaseModel):
    proxy_allowed: bool = False
    attachment_required: bool = True

class SignatureMatrixBinding(BaseModel):
    matrix_name: str
    steps: list[ApprovalStep] = Field(..., min_length=1)

class VersionSequenceConfig(BaseModel):
    skip_alpha_versions: bool = False
    allowed_purposes: list[Literal["DRAFT", "PUBLISHED", "CHANGE_NOTICE"]] = Field(default_factory=lambda: ["DRAFT", "PUBLISHED"])

class ReversalRoleConfig(BaseModel):
    reversal_approver_role: str

class ProjectTemplateConfig(BaseModel):
    record_approval: RecordApprovalConfig | None = None
    stream_approval: StreamApprovalConfig | None = None
    numbering: NumberingConfig | None = None
    customer_approval: CustomerApprovalConfig | None = None
    signature_matrix: SignatureMatrixBinding | None = None
    version_sequence: VersionSequenceConfig | None = None
    reversal_role: ReversalRoleConfig | None = None
```

- [ ] **Step 4: 实现 ProjectTemplateService**

```python
# pcs-backend/app/services/project_template_service.py
from app.schemas.project_template import ProjectTemplateConfig
from app.services.exceptions import PcsError

class ProjectTemplateService:
    @staticmethod
    def validate_config(raw: dict) -> ProjectTemplateConfig:
        """Pydantic v2 验证 default_config_json；失败抛 PcsError(TEMPLATE_CONFIG_INVALID)."""
        try:
            return ProjectTemplateConfig.model_validate(raw)
        except Exception as e:
            raise PcsError(
                f"ProjectTemplate.default_config_json 验证失败：{e}",
                code="TEMPLATE_CONFIG_INVALID",
                status=422,
                details={"validation_error": str(e), "raw_keys": list(raw.keys())},
            ) from e

    @classmethod
    def load(cls, db, template_id):
        """加载 ProjectTemplate，自动验证 default_config_json。"""
        from app.models.config_domain import ProjectTemplate
        tpl = db.get(ProjectTemplate, template_id)
        if not tpl:
            raise PcsError(f"ProjectTemplate {template_id} 不存在", code="TEMPLATE_NOT_FOUND", status=404)
        if tpl.default_config_json:
            cls.validate_config(tpl.default_config_json)
        return tpl
```

- [ ] **Step 5: 运行测试确认 PASS**

Run: `cd pcs-backend && pytest tests/schemas/test_project_template_schema.py -v`
Expected: 9 PASS

- [ ] **Step 6: 跑全量 Pydantic ↔ ORM CI**

Run: `cd pcs-backend && pytest tests/test_pydantic_orm_ci.py -v`（如不存在则跳过）
Expected: PASS（确保 Pydantic 模型未与 ORM 字段冲突）

- [ ] **Step 7: 提交**

```bash
git add pcs-backend/app/schemas/project_template.py \
        pcs-backend/app/services/project_template_service.py \
        pcs-backend/tests/schemas/test_project_template_schema.py
git commit -m "feat(p2-s1.8): ProjectTemplate.default_config_json Pydantic v2 schema 验证（7 子配置）"
```

---

**Sprint 1.8 完成标准**：
- 4 个 Task 全部独立 commit
- 全量 pytest PASS（preconditions 8 类 + FormulaSecurity 2 类 + seed 8 类 + schema 9 类 = 27 新测）
- 本体论 V1.6 §2.3 preconditions 表达力边界声明落地
- P0/P1 偏差 #1, #2, #5, #7 全部闭环

---

# Sprint 1.10 V1.4 P2-OPEN-005 一次性收敛

## Task 1.10.1: 折标煤系数组（V1.4 新增）

**Files:**
- Modify: `pcs-backend/app/models/config_domain.py`（追加 `ToeConversionFactor` 类）
- New: `pcs-backend/alembic/versions/XXXX_add_toe_conversion.py`
- New: `pcs-backend/app/services/toe_conversion_service.py`
- Modify: `pcs-backend/app/api/v1/config.py`（追加 `/config/toe-conversion` 端点）
- Test: `pcs-backend/tests/services/test_toe_conversion_service.py`

**Interfaces:**
- Consumes: `ToeConversionFactor.fuel_type ∈ {GAS, DIESEL, COAL, STEAM, ELECTRICITY, OTHER}`，按 `effective_year` 查询
- Produces: `ToeConversionService.query_by_fuel_year(fuel_type, year) -> ToeConversionFactor`

- [ ] **Step 1: 写失败测试**

```python
# pcs-backend/tests/services/test_toe_conversion_service.py
import pytest
from app.services.toe_conversion_service import ToeConversionService
from app.db.session import get_async_session_factory

@pytest.mark.asyncio
@pytest.fixture(autouse=True)
async def seed_toe():
    factory = get_async_session_factory()
    async with factory() as session:
        await ToeConversionService.seed_defaults(session)

@pytest.mark.asyncio
async def test_seed_defaults_creates_six_fuels():
    """默认 seed 6 个 fuel_type（GAS/DIESEL/COAL/STEAM/ELECTRICITY/OTHER）"""
    from sqlalchemy import select, distinct
    from app.models.config_domain import ToeConversionFactor
    factory = get_async_session_factory()
    async with factory() as session:
        await ToeConversionService.seed_defaults(session)
        result = await session.execute(select(distinct(ToeConversionFactor.fuel_type)))
        fuels = result.scalars().all()
    assert len(fuels) == 6

@pytest.mark.asyncio
async def test_query_by_fuel_year_returns_latest():
    """按 fuel_type + year 查询返回该年份生效值"""
    factory = get_async_session_factory()
    async with factory() as session:
        result = await ToeConversionService.query_by_fuel_year(session, "GAS", 2026)
    assert result.toe_conversion_factor > 0
    assert result.standard_coal_factor > 0

@pytest.mark.asyncio
async def test_query_missing_year_returns_latest_known():
    """未来年份无 seed 时返回已知的最近一年"""
    factory = get_async_session_factory()
    async with factory() as session:
        result = await ToeConversionService.query_by_fuel_year(session, "GAS", 2099)
    assert result.effective_year <= 2026

@pytest.mark.asyncio
async def test_unique_constraint_on_fuel_year():
    """同一 fuel_type + effective_year 唯一"""
    from app.services.exceptions import PcsError
    factory = get_async_session_factory()
    async with factory() as session:
        with pytest.raises(PcsError) as exc:
            await ToeConversionService.create(session, fuel_type="GAS",
                                              toe_conversion_factor=1.0,
                                              standard_coal_factor=1.5,
                                              effective_year=2026)
    assert exc.value.code == "TOE_DUPLICATE"
```

- [ ] **Step 2: 运行测试确认 FAIL**

Run: `cd pcs-backend && pytest tests/services/test_toe_conversion_service.py -v`
Expected: failures

- [ ] **Step 3: 模型 + 迁移**

```python
# pcs-backend/app/models/config_domain.py 追加
from sqlalchemy import Numeric

class ToeConversionFactor(TimestampMixin, Base):
    """折标煤系数组（V1.4 P2-OPEN-005，配套 auxiliary_consumption + utility_energy_summary）。

    fuel_type 枚举：GAS/DIESEL/COAL/STEAM/ELECTRICITY/OTHER。
    effective_year + effective_from / effective_to 控制生效区间。
    """
    __tablename__ = "pcs_toe_conversion_factors"
    __table_args__ = (
        UniqueConstraint("fuel_type", "effective_year", name="uq_toe_fuel_year"),
    )
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    fuel_type: Mapped[str] = mapped_column(String(30), index=True,
        comment="GAS/DIESEL/COAL/STEAM/ELECTRICITY/OTHER")
    toe_conversion_factor: Mapped[float] = mapped_column(Numeric(10, 4),
        comment="吨油当量折算系数")
    standard_coal_factor: Mapped[float] = mapped_column(Numeric(10, 4),
        comment="标煤折算系数")
    effective_year: Mapped[int] = mapped_column(Integer, index=True)
    effective_from: Mapped[datetime.date] = mapped_column(Date)
    effective_to: Mapped[datetime.date | None] = mapped_column(Date, nullable=True)
    source: Mapped[str | None] = mapped_column(String(200))
    version: Mapped[str] = mapped_column(String(50), default="TOE-V1.0")
```

```python
# pcs-backend/alembic/versions/XXXX_add_toe_conversion.py
"""add pcs_toe_conversion_factors

Revision ID: XXXX_add_toe_conversion
Revises: <前一迁移 head>
Create Date: 2026-09-03

V1.4 P2-OPEN-005：折标煤系数组，配套 SUP-008 §2.5 + SUP-010 §3.3.4。
"""
from alembic import op
import sqlalchemy as sa

revision = "XXXX_add_toe_conversion"
down_revision = "<前一迁移 head>"

def upgrade():
    op.create_table(
        "pcs_toe_conversion_factors",
        sa.Column("id", sa.Integer, primary_key=True, autoincrement=True),
        sa.Column("fuel_type", sa.String(30), nullable=False, index=True),
        sa.Column("toe_conversion_factor", sa.Numeric(10, 4), nullable=False),
        sa.Column("standard_coal_factor", sa.Numeric(10, 4), nullable=False),
        sa.Column("effective_year", sa.Integer, nullable=False, index=True),
        sa.Column("effective_from", sa.Date, nullable=False),
        sa.Column("effective_to", sa.Date, nullable=True),
        sa.Column("source", sa.String(200), nullable=True),
        sa.Column("version", sa.String(50), nullable=False, server_default="TOE-V1.0"),
        sa.Column("created_at", sa.DateTime, nullable=False, server_default=sa.func.now()),
        sa.Column("updated_at", sa.DateTime, nullable=False, server_default=sa.func.now()),
        sa.UniqueConstraint("fuel_type", "effective_year", name="uq_toe_fuel_year"),
    )
    # 默认 seed 6 个 fuel_type × 2026 年（基于蜡油加氢—综合能耗.xlsx 实例）
    op.execute("""
        INSERT INTO pcs_toe_conversion_factors
            (fuel_type, toe_conversion_factor, standard_coal_factor,
             effective_year, effective_from, source, version)
        VALUES
            ('GAS',         1.0000, 1.2143, 2026, '2026-01-01', 'GB 2589-2020 / 综合能耗计算通则', 'TOE-V1.0'),
            ('DIESEL',      1.4571, 1.7714, 2026, '2026-01-01', 'GB 2589-2020 / 综合能耗计算通则', 'TOE-V1.0'),
            ('COAL',        0.7143, 0.8667, 2026, '2026-01-01', 'GB 2589-2020 / 综合能耗计算通则', 'TOE-V1.0'),
            ('STEAM',       0.1429, 0.1714, 2026, '2026-01-01', 'GB 2589-2020 / 综合能耗计算通则', 'TOE-V1.0'),
            ('ELECTRICITY', 0.1229, 0.1486, 2026, '2026-01-01', 'GB 2589-2020 / 综合能耗计算通则', 'TOE-V1.0'),
            ('OTHER',       1.0000, 1.2143, 2026, '2026-01-01', 'GB 2589-2020 / 综合能耗计算通则', 'TOE-V1.0')
    """)

def downgrade():
    op.drop_table("pcs_toe_conversion_factors")
```

- [ ] **Step 4: Service 实现（async classmethod，匹配 config.py 端点风格）**

```python
# pcs-backend/app/services/toe_conversion_service.py
from __future__ import annotations
import datetime as dt
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.models.config_domain import ToeConversionFactor
from app.services.exceptions import PcsError

VALID_FUEL_TYPES = {"GAS", "DIESEL", "COAL", "STEAM", "ELECTRICITY", "OTHER"}

class ToeConversionService:
    @classmethod
    async def seed_defaults(cls, session: AsyncSession) -> int:
        """幂等 seed 6 个 fuel_type 默认值（与 Alembic 迁移同步）。"""
        result = await session.execute(select(ToeConversionFactor))
        if result.first():
            return 0
        defaults = [
            ("GAS", 1.0000, 1.2143),
            ("DIESEL", 1.4571, 1.7714),
            ("COAL", 0.7143, 0.8667),
            ("STEAM", 0.1429, 0.1714),
            ("ELECTRICITY", 0.1229, 0.1486),
            ("OTHER", 1.0000, 1.2143),
        ]
        for fuel, toe, coal in defaults:
            session.add(ToeConversionFactor(
                fuel_type=fuel, toe_conversion_factor=toe,
                standard_coal_factor=coal, effective_year=2026,
                effective_from=dt.date(2026, 1, 1),
                source="GB 2589-2020 / 综合能耗计算通则",
            ))
        await session.commit()
        return len(defaults)

    @classmethod
    async def query_by_fuel_year(
        cls, session: AsyncSession, fuel_type: str, year: int
    ) -> ToeConversionFactor:
        """按 fuel_type + year 查询；year 超出时返回最近的已知年。"""
        if fuel_type not in VALID_FUEL_TYPES:
            raise PcsError(f"未知 fuel_type: {fuel_type}", code="TOE_INVALID_FUEL", status=422)
        stmt = (
            select(ToeConversionFactor)
            .where(ToeConversionFactor.fuel_type == fuel_type)
            .where(ToeConversionFactor.effective_year <= year)
            .order_by(ToeConversionFactor.effective_year.desc())
            .limit(1)
        )
        result = (await session.execute(stmt)).scalar_one_or_none()
        if not result:
            raise PcsError(f"无 {fuel_type} 折标煤系数", code="TOE_NOT_FOUND", status=404)
        return result

    @classmethod
    async def create(
        cls, session: AsyncSession, *, fuel_type: str,
        toe_conversion_factor: float, standard_coal_factor: float,
        effective_year: int,
    ) -> ToeConversionFactor:
        if fuel_type not in VALID_FUEL_TYPES:
            raise PcsError(f"未知 fuel_type: {fuel_type}", code="TOE_INVALID_FUEL", status=422)
        existing = (await session.execute(
            select(ToeConversionFactor)
            .where(ToeConversionFactor.fuel_type == fuel_type)
            .where(ToeConversionFactor.effective_year == effective_year)
        )).scalar_one_or_none()
        if existing:
            raise PcsError(
                f"{fuel_type} {effective_year} 年系数已存在", code="TOE_DUPLICATE", status=409,
            )
        new_row = ToeConversionFactor(
            fuel_type=fuel_type, toe_conversion_factor=toe_conversion_factor,
            standard_coal_factor=standard_coal_factor,
            effective_year=effective_year,
            effective_from=dt.date(effective_year, 1, 1),
        )
        session.add(new_row)
        await session.commit()
        return new_row
```

- [ ] **Step 5: 端点（async，匹配 config.py 风格）**

```python
# pcs-backend/app/api/v1/config.py 追加（async def + AsyncSession）
from app.services.toe_conversion_service import ToeConversionService

@router.get("/config/toe-conversion")
async def get_toe_conversion(
    fuel_type: str, year: int,
    db: AsyncSession = Depends(get_async_db),  # 复用既有 get_async_db 或从 db/session.py 引入
):
    """按 fuel_type + year 查询折标煤系数（V1.4 P2-OPEN-005）"""
    row = await ToeConversionService.query_by_fuel_year(db, fuel_type, year)
    return {
        "fuel_type": row.fuel_type,
        "toe_conversion_factor": float(row.toe_conversion_factor),
        "standard_coal_factor": float(row.standard_coal_factor),
        "effective_year": row.effective_year,
        "source": row.source,
    }
```

- [ ] **Step 6: 运行测试确认 PASS**

Run: `cd pcs-backend && pytest tests/services/test_toe_conversion_service.py -v`
Expected: 4 PASS

- [ ] **Step 7: 提交**

```bash
git add pcs-backend/app/models/config_domain.py \
        pcs-backend/alembic/versions/XXXX_add_toe_conversion.py \
        pcs-backend/app/services/toe_conversion_service.py \
        pcs-backend/app/api/v1/config.py \
        pcs-backend/tests/services/test_toe_conversion_service.py
git commit -m "feat(p2-s1.10): 折标煤系数组（V1.4 P2-OPEN-005）+ Alembic 迁移 + API"
```

---

## Task 1.10.2: DETAIL 模板资产入库

**Files:**
- Modify: `pcs-backend/app/models/config_domain.py:119-135`（`TemplateFile.template_version_seq` 字段插入在 `version` 后）
- New: `pcs-backend/app/seeds/templates/detail_121_a_101.json`
- New: `pcs-backend/app/seeds/templates/detail_131_e_102_eor.json`
- Modify: `pcs-backend/app/services/template_service.py:38-60`（在现有 `async def upload` 内追加 `template_version_seq` 自动续号逻辑；保持 `async` 签名）
- Test: `pcs-backend/tests/seeds/test_detail_templates.py`

**Interfaces:**
- Consumes: 两份 .xls 文件元数据（行/列/HTRI 版本/设备类型）
- Produces: `TemplateFile.template_version_seq` 自动续号；DETAIL 模板元数据 seed

> **R6 (reconcile with existing async pattern)**: 现有 `template_service.py` 第 38-60 行是 `async def upload(self, *, asset_id, file_bytes, file_name, file_type)` 实例方法，使用 `AsyncSession`。计划中的 `@classmethod` sync 版本会破坏现有调用方。实施时**修改现有 async upload，在 `await self.session.flush()` 后追加查询 max(seq) 并设值**，不替换原方法。

- [ ] **Step 1: 写失败测试**

```python
# pcs-backend/tests/seeds/test_detail_templates.py
import pytest
import uuid
from pathlib import Path
from app.services.template_service import TemplateService
from app.db.session import get_async_session_factory

@pytest.mark.asyncio
async def test_upload_auto_increments_seq():
    """模板上传时 template_version_seq 自动递增（async instance method）"""
    factory = get_async_session_factory()
    async with factory() as session:
        svc = TemplateService(session=session, storage_root=Path("/tmp/seq_test"))
        first = await svc.upload(
            asset_id=uuid.uuid4(),
            file_bytes=b"x", file_name="tpl1.xlsx", file_type="xlsx",
        )
    async with factory() as session:
        svc = TemplateService(session=session, storage_root=Path("/tmp/seq_test"))
        second = await svc.upload(
            asset_id=uuid.uuid4(),
            file_bytes=b"y", file_name="tpl2.xlsx", file_type="xlsx",
        )
    assert second.template_version_seq == first.template_version_seq + 1

def test_detail_121_a_101_metadata_loadable():
    """加载 121-A-101 DETAIL 模板元数据"""
    from app.services.template_service import load_seed_metadata
    metadata = load_seed_metadata("detail_121_a_101")
    assert metadata["device_type"] == "ACHE"  # 空冷器
    assert metadata["column_count"] == 33
    assert metadata["htri_version"]

def test_detail_131_e_102_eor_metadata_loadable():
    metadata = load_seed_metadata("detail_131_e_102_eor")
    assert metadata["device_type"] == "SHELL_TUBE"  # 管壳式
    assert metadata["column_count"] == 23
```

- [ ] **Step 2: 运行测试确认 FAIL**

Run: `cd pcs-backend && pytest tests/seeds/test_detail_templates.py -v`
Expected: failures

- [ ] **Step 3: 模型修改**

```python
# pcs-backend/app/models/config_domain.py TemplateFile 类追加字段
template_version_seq: Mapped[int] = mapped_column(
    Integer, default=0,
    comment="V1.4 P2-OPEN-005：模板版本号字段纳入 CONFIG 模板版本序列（自增）",
)
```

- [ ] **Step 4: Alembic 迁移**

```python
# pcs-backend/alembic/versions/XXXX_add_template_version_seq.py
"""add TemplateFile.template_version_seq

Revision ID: XXXX_add_template_version_seq
Revises: XXXX_add_toe_conversion
Create Date: 2026-09-03
"""
from alembic import op
import sqlalchemy as sa

revision = "XXXX_add_template_version_seq"
down_revision = "XXXX_add_toe_conversion"

def upgrade():
    op.add_column(
        "template_files",
        sa.Column("template_version_seq", sa.Integer, nullable=False, server_default="0",
                  comment="V1.4 P2-OPEN-005：模板版本序列号"),
    )

def downgrade():
    op.drop_column("template_files", "template_version_seq")
```

- [ ] **Step 5: 元数据 seed JSON**

```json
// pcs-backend/app/seeds/templates/detail_121_a_101.json
{
  "template_id": "DETAIL-ACHE-121-A-101",
  "device_type": "ACHE",
  "name": "121-A-101 HTRI 空冷器 DETAIL 模板",
  "source_file": "121-A-101.xls",
  "htri_version": "HTRI Xist 5.0",
  "column_count": 33,
  "columns": [
    {"name": "Tag",                "type": "string", "required": true,  "unit": ""},
    {"name": "Service",            "type": "string", "required": true,  "unit": ""},
    {"name": "Duty",               "type": "float",  "required": true,  "unit": "kW"},
    {"name": "Hot Inlet T",        "type": "float",  "required": true,  "unit": "°C"},
    {"name": "Hot Outlet T",       "type": "float",  "required": true,  "unit": "°C"},
    {"name": "Hot Flow Rate",      "type": "float",  "required": true,  "unit": "kg/h"},
    {"name": "Air Inlet T",        "type": "float",  "required": true,  "unit": "°C"},
    {"name": "Air Outlet T",       "type": "float",  "required": true,  "unit": "°C"},
    {"name": "Air Flow Rate",      "type": "float",  "required": true,  "unit": "kg/h"},
    {"name": "Design Press",       "type": "float",  "required": true,  "unit": "MPaG"},
    {"name": "Design Temp",        "type": "float",  "required": true,  "unit": "°C"},
    {"name": "Number of Bays",     "type": "int",    "required": true,  "unit": ""},
    {"name": "Number of Fans",     "type": "int",    "required": true,  "unit": ""},
    {"name": "Fan Power",          "type": "float",  "required": true,  "unit": "kW"},
    {"name": "Fin Density",        "type": "float",  "required": false, "unit": "fpi"},
    {"name": "Tube Material",      "type": "string", "required": true,  "unit": ""},
    {"name": "Tube OD",            "type": "float",  "required": true,  "unit": "mm"},
    {"name": "Tube Length",        "type": "float",  "required": true,  "unit": "m"},
    {"name": "Number of Tubes",    "type": "int",    "required": true,  "unit": ""},
    {"name": "Number of Passes",   "type": "int",    "required": true,  "unit": ""},
    {"name": "Bundle Orientation", "type": "enum",   "required": true,  "unit": "HORIZONTAL/VERTICAL"},
    {"name": "Air Side Press Drop","type": "float",  "required": true,  "unit": "Pa"},
    {"name": "Tube Side Press Drop","type": "float", "required": true,  "unit": "Pa"},
    {"name": "Overall U",         "type": "float",  "required": true,  "unit": "W/(m²·K)"},
    {"name": "MTD",                "type": "float",  "required": true,  "unit": "K"},
    {"name": "LMTD",               "type": "float",  "required": true,  "unit": "K"},
    {"name": "Fouling Hot",        "type": "float",  "required": false, "unit": "m²·K/W"},
    {"name": "Fouling Cold",       "type": "float",  "required": false, "unit": "m²·K/W"},
    {"name": "Rec Area Required",  "type": "float",  "required": true,  "unit": "m²"},
    {"name": "Effective Area",     "type": "float",  "required": true,  "unit": "m²"},
    {"name": "Margin",             "type": "float",  "required": true,  "unit": "%"},
    {"name": "Weight",             "type": "float",  "required": true,  "unit": "kg"},
    {"name": "Remarks",            "type": "string", "required": false, "unit": ""}
  ]
}
```

```json
// pcs-backend/app/seeds/templates/detail_131_e_102_eor.json
{
  "template_id": "DETAIL-ST-131-E-102-EOR",
  "device_type": "SHELL_TUBE",
  "name": "131-E-102 EOR TEMA 管壳式 DETAIL 模板",
  "source_file": "131-E-102-EOR.xls",
  "htri_version": "HTRI Xist 5.0",
  "tema_type": "BEM",
  "column_count": 23,
  "columns": [
    {"name": "Tag",                "type": "string", "required": true,  "unit": ""},
    {"name": "Service",            "type": "string", "required": true,  "unit": ""},
    {"name": "Duty",               "type": "float",  "required": true,  "unit": "kW"},
    {"name": "Shell Side Inlet T", "type": "float",  "required": true,  "unit": "°C"},
    {"name": "Shell Side Outlet T","type": "float",  "required": true,  "unit": "°C"},
    {"name": "Tube Side Inlet T",  "type": "float",  "required": true,  "unit": "°C"},
    {"name": "Tube Side Outlet T", "type": "float",  "required": true,  "unit": "°C"},
    {"name": "Shell Flow Rate",    "type": "float",  "required": true,  "unit": "kg/h"},
    {"name": "Tube Flow Rate",     "type": "float",  "required": true,  "unit": "kg/h"},
    {"name": "Shell Design Press", "type": "float",  "required": true,  "unit": "MPaG"},
    {"name": "Tube Design Press",  "type": "float",  "required": true,  "unit": "MPaG"},
    {"name": "Shell ID",           "type": "float",  "required": true,  "unit": "mm"},
    {"name": "Tube OD",            "type": "float",  "required": true,  "unit": "mm"},
    {"name": "Tube Length",        "type": "float",  "required": true,  "unit": "m"},
    {"name": "Number of Tubes",    "type": "int",    "required": true,  "unit": ""},
    {"name": "Tube Pitch",         "type": "float",  "required": true,  "unit": "mm"},
    {"name": "Baffle Spacing",     "type": "float",  "required": true,  "unit": "mm"},
    {"name": "Number of Baffles",  "type": "int",    "required": true,  "unit": ""},
    {"name": "Shell Side Press Drop","type": "float","required": true,  "unit": "Pa"},
    {"name": "Tube Side Press Drop","type": "float", "required": true,  "unit": "Pa"},
    {"name": "Overall U",          "type": "float",  "required": true,  "unit": "W/(m²·K)"},
    {"name": "Effective Area",     "type": "float",  "required": true,  "unit": "m²"},
    {"name": "Weight",             "type": "float",  "required": true,  "unit": "kg"}
  ]
}
```

- [ ] **Step 6: 修改现有 async upload 方法 + 新增 load_seed_metadata 模块函数**

```python
# pcs-backend/app/services/template_service.py 在现有 async upload 内追加 seq 续号
from sqlalchemy import func, select

# 在 import 区域新增
from app.models.htri_template import HtriTemplateSchema  # 仅 Task 1.10.3 改时启用

class TemplateService:
    async def upload(
        self,
        *,
        asset_id: UUID,
        file_bytes: bytes,
        file_name: str,
        file_type: str,
    ) -> TemplateFile:
        sha = hashlib.sha256(file_bytes).hexdigest()
        path = self.storage_root / f"{sha}.{file_name}"
        path.write_bytes(file_bytes)
        # V1.4 P2-OPEN-005：自动续 template_version_seq
        result = await self.session.execute(
            select(func.coalesce(func.max(TemplateFile.template_version_seq), 0))
        )
        next_seq = (result.scalar() or 0) + 1
        tpl = TemplateFile(
            asset_id=asset_id,
            name=file_name,
            file_type=file_type,
            file_path=str(path),
            placeholders_json={"vars": []},
            version="v1",
            template_version_seq=next_seq,
            status="DRAFT",
        )
        self.session.add(tpl)
        await self.session.flush()
        return tpl


# 模块级函数（非实例方法），用于加载 JSON seed 元数据
def load_seed_metadata(template_id: str) -> dict:
    """加载 DETAIL 模板元数据（V1.4 P2-OPEN-005）"""
    from app.services.exceptions import PcsError
    file_map = {
        "detail_121_a_101": Path(__file__).parent.parent / "seeds" / "templates" / "detail_121_a_101.json",
        "detail_131_e_102_eor": Path(__file__).parent.parent / "seeds" / "templates" / "detail_131_e_102_eor.json",
    }
    if template_id not in file_map:
        raise PcsError(f"未知 DETAIL 模板：{template_id}", code="TEMPLATE_NOT_FOUND", status=404)
    return json.loads(file_map[template_id].read_text(encoding="utf-8"))
```

- [ ] **Step 7: 运行测试确认 PASS**

Run: `cd pcs-backend && pytest tests/seeds/test_detail_templates.py -v`
Expected: 3 PASS

- [ ] **Step 8: 提交**

```bash
git add pcs-backend/app/models/config_domain.py \
        pcs-backend/alembic/versions/XXXX_add_template_version_seq.py \
        pcs-backend/app/seeds/templates/detail_121_a_101.json \
        pcs-backend/app/seeds/templates/detail_131_e_102_eor.json \
        pcs-backend/app/services/template_service.py \
        pcs-backend/tests/seeds/test_detail_templates.py
git commit -m "feat(p2-s1.10): DETAIL 模板资产入库（121-A-101 + 131-E-102-EOR）+ template_version_seq 自动续号"
```

---

## Task 1.10.3: HTRI 解析模板 schema（V1.4 配套）

**Files:**
- New: `pcs-backend/app/models/htri_template.py`
- New: `pcs-backend/alembic/versions/XXXX_add_htri_template_schemas.py`
- Modify: `pcs-backend/app/services/template_service.py`
- Test: `pcs-backend/tests/models/test_htri_template_schema.py`

**Interfaces:**
- Consumes: `HtriTemplateSchema.device_type ∈ {ACHE, SHELL_TUBE}`，列级类型/必填/单位
- Produces: `TemplateService.register_htri_schema(device_type, columns_json, version)` → 注册 HTRI schema

- [ ] **Step 1: 写失败测试**

```python
# pcs-backend/tests/models/test_htri_template_schema.py
import pytest
from app.services.template_service import TemplateService
from app.db.session import get_async_session_factory
from pathlib import Path

@pytest.mark.asyncio
async def test_register_htri_schema_ache():
    factory = get_async_session_factory()
    async with factory() as session:
        svc = TemplateService(session=session, storage_root=Path("/tmp/htri_test"))
        schema = await svc.register_htri_schema(
            device_type="ACHE", column_count=33, version="HTRI-V5.0",
            source_file_ref="121-A-101.xls",
        )
    assert schema.device_type == "ACHE"
    assert schema.column_count == 33

@pytest.mark.asyncio
async def test_register_htri_schema_shell_tube():
    factory = get_async_session_factory()
    async with factory() as session:
        svc = TemplateService(session=session, storage_root=Path("/tmp/htri_test"))
        schema = await svc.register_htri_schema(
            device_type="SHELL_TUBE", column_count=23, version="HTRI-V5.0",
            source_file_ref="131-E-102-EOR.xls", tema_type="BEM",
        )
    assert schema.device_type == "SHELL_TUBE"
    assert schema.tema_type == "BEM"

@pytest.mark.asyncio
async def test_register_htri_schema_invalid_device_type():
    from app.services.exceptions import PcsError
    factory = get_async_session_factory()
    async with factory() as session:
        svc = TemplateService(session=session, storage_root=Path("/tmp/htri_test"))
        with pytest.raises(PcsError) as exc:
            await svc.register_htri_schema(device_type="UNKNOWN", column_count=10)
    assert exc.value.code == "HTRI_INVALID_DEVICE_TYPE"
```

- [ ] **Step 2: 运行测试确认 FAIL**

Run: `cd pcs-backend && pytest tests/models/test_htri_template_schema.py -v`
Expected: failures

- [ ] **Step 3: 模型 + 迁移**

```python
# pcs-backend/app/models/htri_template.py
import uuid
from sqlalchemy import String, Integer, Uuid
from sqlalchemy.dialects.postgresql import JSONB
from sqlalchemy.orm import Mapped, mapped_column

from app.db.base import Base
from app.models.mixins import TimestampMixin

VALID_HTRI_DEVICE_TYPES = {"ACHE", "SHELL_TUBE"}

class HtriTemplateSchema(TimestampMixin, Base):
    """HTRI 解析模板 schema（V1.4 P2-OPEN-005）。"""
    __tablename__ = "htri_template_schemas"
    schema_id: Mapped[uuid.UUID] = mapped_column(Uuid, primary_key=True, default=uuid.uuid4)
    device_type: Mapped[str] = mapped_column(String(20), comment="ACHE/SHELL_TUBE")
    column_count: Mapped[int] = mapped_column(Integer)
    columns_json: Mapped[dict] = mapped_column(JSONB,
        comment="[{name, type, required, unit}, ...]")
    tema_type: Mapped[str | None] = mapped_column(String(10), nullable=True,
        comment="BEM/AES/AKT（仅 SHELL_TUBE 适用）")
    version: Mapped[str] = mapped_column(String(50))
    source_file_ref: Mapped[str | None] = mapped_column(String(200),
        comment="原始 .xls 文件引用")
```

```python
# pcs-backend/alembic/versions/XXXX_add_htri_template_schemas.py
"""add htri_template_schemas

Revision ID: XXXX_add_htri_template_schemas
Revises: XXXX_add_template_version_seq
Create Date: 2026-09-03
"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects.postgresql import UUID, JSONB

revision = "XXXX_add_htri_template_schemas"
down_revision = "XXXX_add_template_version_seq"

def upgrade():
    op.create_table(
        "htri_template_schemas",
        sa.Column("schema_id", UUID, primary_key=True),
        sa.Column("device_type", sa.String(20), nullable=False),
        sa.Column("column_count", sa.Integer, nullable=False),
        sa.Column("columns_json", JSONB, nullable=False),
        sa.Column("tema_type", sa.String(10), nullable=True),
        sa.Column("version", sa.String(50), nullable=False),
        sa.Column("source_file_ref", sa.String(200), nullable=True),
        sa.Column("created_at", sa.DateTime, nullable=False, server_default=sa.func.now()),
        sa.Column("updated_at", sa.DateTime, nullable=False, server_default=sa.func.now()),
    )

def downgrade():
    op.drop_table("htri_template_schemas")
```

- [ ] **Step 4: Service 实现**

```python
# pcs-backend/app/services/template_service.py 在 TemplateService 类内新增 async instance method
from app.models.htri_template import HtriTemplateSchema, VALID_HTRI_DEVICE_TYPES

class TemplateService:
    # ... 既有方法 ...

    async def register_htri_schema(
        self,
        *,
        device_type: str,
        column_count: int,
        version: str = "HTRI-V1.0",
        columns_json: dict | None = None,
        source_file_ref: str | None = None,
        tema_type: str | None = None,
    ) -> HtriTemplateSchema:
        """注册 HTRI 解析模板 schema（V1.4 P2-OPEN-005，async instance method 匹配 TemplateService 现有签名）"""
        if device_type not in VALID_HTRI_DEVICE_TYPES:
            raise PcsError(
                f"未知 HTRI device_type: {device_type}", code="HTRI_INVALID_DEVICE_TYPE", status=422,
                details={"valid_types": sorted(VALID_HTRI_DEVICE_TYPES)},
            )
        schema = HtriTemplateSchema(
            device_type=device_type, column_count=column_count,
            columns_json=columns_json or {}, version=version,
            source_file_ref=source_file_ref, tema_type=tema_type,
        )
        self.session.add(schema)
        await self.session.flush()
        return schema
```

- [ ] **Step 5: 运行测试确认 PASS**

Run: `cd pcs-backend && pytest tests/models/test_htri_template_schema.py -v`
Expected: 3 PASS

- [ ] **Step 6: 提交**

```bash
git add pcs-backend/app/models/htri_template.py \
        pcs-backend/alembic/versions/XXXX_add_htri_template_schemas.py \
        pcs-backend/app/services/template_service.py \
        pcs-backend/tests/models/test_htri_template_schema.py
git commit -m "feat(p2-s1.10): HTRI 解析模板 schema（ACHE 33 列 + SHELL_TUBE 23 列）"
```

---

## Task 1.10.4: ADR-0029 + DICT V3.9 同步

**Files:**
- New: `docs/adr/0029-toe-conversion-and-detail-htri-templates.md`
- Modify: `docs/dict/DICT-ALL-003.md`（V3.8 → V3.9）

**Interfaces:**
- Consumes: 决策点 3 项（折标煤归 CATEGORY_3 / DETAIL 模板归 CATEGORY_4 / HTRI schema 独立表）
- Produces: ADR 文档 + DICT V3.9

- [ ] **Step 1: 起草 ADR-0029**

```markdown
<!-- docs/adr/0029-toe-conversion-and-detail-htri-templates.md -->
# ADR-0029：折标煤系数组与 DETAIL/HTRI 模板入库

- 状态：起草中（V1.4 P2-OPEN-005 评审）
- 日期：2026-09-03
- 决策者：联合项目组 + 工艺部
- 触发：P2 V1.4 spec 升版 + 偏差审计（commit 35a1614）

## 背景

P2 V1.4 P2-OPEN-005 增补要求：
- CONFIG CATEGORY_3 新增折标煤系数组（toe_conversion_factor / standard_coal_factor）
- CONFIG CATEGORY_4 描述扩展 DETAIL 模板 + HTRI 解析模板
- 模板版本号字段纳入 CONFIG 模板版本序列

须要解决 3 项架构决策。

## 决策

### 决策 1：折标煤系数组归 CATEGORY_3（经验系数管理）
- **理由**：折标煤系数组（toe / standard_coal 系数）属工程经验值，调用方 UTIL 模块按 fuel_type + effective_year 引用
- **替代方案**：归 CATEGORY_5（标准数据库）——否决，因折标煤系数随燃料类型与年份变化频繁，需走 CONFIG 5 态审批而非字典只读

### 决策 2：DETAIL 模板资产归 CATEGORY_4（报表与导入模板管理）
- **理由**：121-A-101.xls / 131-E-102-EOR.xls 是 .xls 模板资产，与现有 Jinja2 / .dotx / .xltx 模板同等地位
- **替代方案**：新建 CATEGORY_7（DETAIL 模板专属）——否决，超出六类框架；DETAIL 仅是 detail_level=DETAIL 的模板实例

### 决策 3：HTRI schema 独立表 htri_template_schemas
- **理由**：列级 schema 需 JSONB 灵活扩展 + 独立版本序列 + 与 TemplateFile 弱耦合
- **替代方案**：塞入 TemplateFile.columns_json JSONB——否决，TemplateFile 主键是模板文件元数据，HTRI schema 主键是列级 schema 元数据，混淆会破坏可追溯性

## 影响

- 新表 2 张：pcs_toe_conversion_factors / htri_template_schemas
- TemplateFile 增字段：template_version_seq
- DICT V3.8 → V3.9 同步
- CATEGORY_3 seed 6 张默认表 + 折标煤系数（基于蜡油加氢—综合能耗.xlsx 实例）

## 后续

- ADR 评审通过后执行 Task 1.10.1 + 1.10.2 + 1.10.3
- DICT V3.9 升版 commit 同步推送
```

- [ ] **Step 2: DICT V3.9 升版**

```markdown
<!-- docs/dict/DICT-ALL-003.md 修改版本历史追加 -->
| V3.9 | 2026-09-03 | P2 V1.4 P2-OPEN-005 一次性收敛：+ pcs_toe_conversion_factors 表（6 fuel_type seed） + htri_template_schemas 表（ACHE 33 列 + SHELL_TUBE 23 列） + TemplateFile.template_version_seq 字段 + CATEGORY_3 折标煤 seed；ADR-0029 同步 | 联合项目组 |
```

并追加：

- 表 `pcs_toe_conversion_factors`（V3.9 新增）字段清单
- 表 `htri_template_schemas`（V3.9 新增）字段清单
- TemplateFile.template_version_seq（V3.9 字段新增）

- [ ] **Step 3: 启动 ADR 评审**

联合项目组 + 工艺部双签。评审通过前 1.10.4 不 commit。

- [ ] **Step 4: 提交**

```bash
git add docs/adr/0029-toe-conversion-and-detail-htri-templates.md docs/dict/DICT-ALL-003.md
git commit -m "docs(adr): ADR-0029 + DICT V3.9（P2 V1.4 折标煤 + DETAIL/HTRI 模板收敛）"
```

---

**Sprint 1.10 完成标准**：
- 4 个 Task 全部独立 commit
- 全量 pytest PASS（折标煤 4 类 + DETAIL 3 类 + HTRI 3 类 = 10 新测）
- ADR-0029 评审通过 + DICT V3.9 升版
- 本体论 V1.6 §3.5 规则清单（方案 A/B 二选一）若已选定 A，可补 register_htri_schema 规则入 Pydantic schema 验证
- V1.4 P2-OPEN-005 完全闭环

---

# 不在本计划范围（明确排除）

| 偏差 # | 描述 | 归属 |
|---|---|---|
| #4 | PipeClass service + 5 端点全缺 | Sprint 1.9 |
| #6 | Riazi-Daubert / 虚拟组分切割 service 缺 | Sprint 1.9 |
| equip-lib | 沉淀 service 缺 | Sprint 1.9 |
| #10 | formula test/preview 端点补齐 | Sprint 1.12 |
| #13 | 有效版本缓存 | Sprint 1.12 |
| #11 | 版本 diff AST 化 | Sprint 1.11 |
| #8/9 | version_sequence_config / 撤销批准 / CHANGE_NOTICE_3_LEVEL seed | Sprint 1.11 |

---

# 自检（spec 对照）

- [x] **Spec coverage**：P2 V1.4 §3.2.2a preconditions（V1.3 治理条款）→ Task 1.8.1；PcsError 统一（V1.3）→ Task 1.8.2；CATEGORY_3 seed（V1.0）→ Task 1.8.3；default_config_json schema 验证（V1.1 SUP-007）→ Task 1.8.4；V1.4 P2-OPEN-005（折标煤 + DETAIL/HTRI 模板）→ Task 1.10.1/1.10.2/1.10.3；ADR-0029 + DICT V3.9 → Task 1.10.4。
- [x] **Placeholder scan**：无 TBD / TODO / "implement later" / "similar to" 标记。
- [x] **Type consistency**：`PreconditionViolation` / `FormulaSecurityError` / `PcsError` 在 exceptions.py 统一定义；`ToeConversionFactor` / `HtriTemplateSchema` 模型字段与 service 接口一致；`template_version_seq` 在 model + migration + service + test 同步。

---

# 执行交接

**Plan complete and saved to `docs/superpowers/plans/2026-09-03-p2-sprint-1.8-and-1.10.md`.**

**Two execution options:**

1. **Subagent-Driven (recommended)** — 我调度 fresh subagent per task，每 task 单独 review（spec compliance + code quality），wall-clock ≈ 5 个 task × 1–2 hours = 5–10 人天
2. **Inline Execution** — 在当前会话执行，batch execution with checkpoints

**Which approach?**